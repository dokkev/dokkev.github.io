<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://dongho.dev/feed.xml" rel="self" type="application/atom+xml"/><link href="https://dongho.dev/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-23T20:44:30+00:00</updated><id>https://dongho.dev/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry xml:lang="en"><title type="html">[WBC 04] - Task Priority: Nullspace Projection vs Weighted QP vs HQP</title><link href="https://dongho.dev/wbc04/" rel="alternate" type="text/html" title="[WBC 04] - Task Priority: Nullspace Projection vs Weighted QP vs HQP"/><published>2026-06-16T00:00:00+00:00</published><updated>2026-06-16T00:00:00+00:00</updated><id>https://dongho.dev/wbc04</id><content type="html" xml:base="https://dongho.dev/wbc04/"><![CDATA[<h1 id="wbc04-task-priority-nullspace-projection-vs-weighted-qp-vs-hqp">WBC04. Task Priority: Nullspace Projection vs Weighted QP vs HQP</h1> <p>In the previous post, we looked at how an SE(3) task becomes an acceleration-level constraint.</p> <p>A pose target is not sent directly to the WBC solver. The task computes pose error, velocity error, desired task acceleration, Jacobian, and drift. Then it gives the solver a relation like:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>J_task(q) qddot = a_des - drift
</code></pre></div></div> <p>That works when we look at one task.</p> <p>But whole-body control is not about one task.</p> <p>A humanoid may need to control several objectives at the same time:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>center of mass
base orientation
left foot contact
right foot contact
hand pose
posture
joint limits
torque limits
contact forces
</code></pre></div></div> <p>All of these tasks ultimately depend on the same robot motion.</p> <p>They share the same generalized acceleration:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>qddot
</code></pre></div></div> <p>So the next problem is unavoidable:</p> <blockquote> <p>What happens when tasks conflict?</p> </blockquote> <p>If the hand wants to move forward, but doing so would disturb balance, which task should win?</p> <p>If a posture task wants to straighten the knee, but the foot contact constraint requires the knee to stay bent, which objective should be preserved?</p> <p>This is where task priority becomes central.</p> <p>There are several ways to represent priority in WBC. In this post, we compare three common approaches:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. Weighted QP
2. Nullspace projection
3. Hierarchical QP
</code></pre></div></div> <p>These methods are often presented as mathematical alternatives, but they also lead to different software architectures.</p> <p>The key question is not simply:</p> <blockquote> <p>Which one is better?</p> </blockquote> <p>The better question is:</p> <blockquote> <p>What kind of priority guarantee do we need, and what constraints must the controller handle?</p> </blockquote> <hr/> <h2 id="1-the-task-conflict-problem">1. The task conflict problem</h2> <p>Suppose we have two acceleration-level tasks:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>J1 qddot = a1
J2 qddot = a2
</code></pre></div></div> <p>If there exists a <code class="language-plaintext highlighter-rouge">qddot</code> that satisfies both equations, life is easy.</p> <p>But in whole-body control, that is often not the case.</p> <p>The robot has finite degrees of freedom. Contacts restrict motion. Torque limits restrict feasible acceleration. Friction constraints restrict contact forces. Some tasks may simply be incompatible at a given instant.</p> <p>For example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Task 1:
  keep the center of mass stable

Task 2:
  move the hand to a far target

Task 3:
  keep posture close to nominal
</code></pre></div></div> <p>If the hand target is too far, the robot may not be able to reach it without moving the CoM or violating a contact constraint.</p> <p>So WBC needs a rule for resolving conflicts.</p> <p>There are two broad philosophies:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Soft priority:
  important tasks get larger weights, but all tasks are optimized together.

Strict priority:
  lower-priority tasks are only allowed to use motion that does not disturb higher-priority tasks.
</code></pre></div></div> <p>Weighted QP belongs to the first category.</p> <p>Nullspace projection and HQP are closer to the second category.</p> <hr/> <h2 id="2-weighted-qp-priority-as-cost-weight">2. Weighted QP: priority as cost weight</h2> <p>The most straightforward approach is to put all task errors into one optimization problem.</p> <p>For example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>minimize over qddot

  w1 || J1 qddot - a1 ||²
+ w2 || J2 qddot - a2 ||²
+ w3 || J3 qddot - a3 ||²
</code></pre></div></div> <p>Here, task priority is represented by weights.</p> <p>If task 1 is more important than task 2, we choose:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>w1 &gt;&gt; w2
</code></pre></div></div> <p>In a full inverse-dynamics WBC formulation, this objective may be combined with dynamics, torque limits, friction cones, and contact force constraints.</p> <p>The software structure is relatively simple:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">solver</span><span class="p">.</span><span class="n">addCost</span><span class="p">(</span><span class="n">com_task</span><span class="p">,</span> <span class="n">weight_com</span><span class="p">);</span>
<span class="n">solver</span><span class="p">.</span><span class="n">addCost</span><span class="p">(</span><span class="n">hand_task</span><span class="p">,</span> <span class="n">weight_hand</span><span class="p">);</span>
<span class="n">solver</span><span class="p">.</span><span class="n">addCost</span><span class="p">(</span><span class="n">posture_task</span><span class="p">,</span> <span class="n">weight_posture</span><span class="p">);</span>

<span class="n">solver</span><span class="p">.</span><span class="n">addConstraint</span><span class="p">(</span><span class="n">dynamics</span><span class="p">);</span>
<span class="n">solver</span><span class="p">.</span><span class="n">addConstraint</span><span class="p">(</span><span class="n">contact_constraints</span><span class="p">);</span>
<span class="n">solver</span><span class="p">.</span><span class="n">addConstraint</span><span class="p">(</span><span class="n">torque_limits</span><span class="p">);</span>

<span class="n">solution</span> <span class="o">=</span> <span class="n">solver</span><span class="p">.</span><span class="n">solve</span><span class="p">();</span>
</code></pre></div></div> <p>This is one reason weighted QP is attractive.</p> <p>Everything goes into one QP.</p> <p>Tasks are costs. Physical limits are constraints. The solver returns the best compromise.</p> <h3 id="advantages">Advantages</h3> <p>Weighted QP is easy to formulate and implement compared to strict hierarchy methods.</p> <p>It also handles inequality constraints naturally. Torque limits, friction cones, unilateral contact constraints, and joint limits can be added as constraints in the same optimization problem.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tau_min &lt;= tau &lt;= tau_max
friction_cone(lambda) &gt;= 0
joint_limit_lower &lt;= q &lt;= joint_limit_upper
</code></pre></div></div> <p>This makes weighted QP practical for real robot systems.</p> <p>It is also flexible. If a task should matter more, increase its weight. If a posture objective should be soft, give it a lower weight.</p> <h3 id="limitations">Limitations</h3> <p>The main limitation is that weights do not create strict priority.</p> <p>A large weight means:</p> <blockquote> <p>This task is expensive to violate.</p> </blockquote> <p>It does not mean:</p> <blockquote> <p>This task can never be violated by lower-priority tasks.</p> </blockquote> <p>If all tasks are in the same objective, then the optimizer can still trade errors between them.</p> <p>For example, a lower-priority hand task can slightly disturb a higher-priority balance task if the cost tradeoff allows it.</p> <p>This is not always bad. In some systems, a soft compromise is exactly what we want.</p> <p>But it is not the same as hierarchy.</p> <p>Weighted QP also introduces tuning problems.</p> <p>Weights depend on units, task dimensions, numerical scaling, and solver conditioning.</p> <p>A position task measured in meters, an orientation task measured in radians, a force task measured in newtons, and a momentum task do not naturally live on the same scale.</p> <p>So a weight is not just a priority number.</p> <p>It is also a numerical scaling parameter.</p> <p>This can make tuning difficult.</p> <h3 id="summary">Summary</h3> <p>Weighted QP represents priority through the objective function.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Priority = cost weight
</code></pre></div></div> <p>It is simple, flexible, and constraint-friendly.</p> <p>But priority is soft.</p> <hr/> <h2 id="3-nullspace-projection-priority-as-motion-subspace">3. Nullspace projection: priority as motion subspace</h2> <p>Nullspace projection takes a different view.</p> <p>Instead of putting all tasks into one cost, it solves tasks in priority order.</p> <p>First, solve the high-priority task.</p> <p>Then, solve the lower-priority task only in the null space of the higher-priority task.</p> <p>For a first task:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>J1 qddot = a1
</code></pre></div></div> <p>a simple solution is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>qddot1 = J1# a1
</code></pre></div></div> <p>where <code class="language-plaintext highlighter-rouge">J1#</code> is some generalized inverse.</p> <p>The nullspace projector is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>N1 = I - J1# J1
</code></pre></div></div> <p>Any motion inside <code class="language-plaintext highlighter-rouge">N1</code> does not affect task 1.</p> <p>So a second task can be added as:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>qddot = qddot1 + N1 z
</code></pre></div></div> <p>where <code class="language-plaintext highlighter-rouge">z</code> is chosen to reduce task 2 error.</p> <p>A common form is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>qddot2 =
qddot1
+ N1 (J2 N1)# (a2 - J2 qddot1)
</code></pre></div></div> <p>The exact expression depends on the chosen inverse and dynamics consistency, but the idea is the same:</p> <blockquote> <p>Task 2 is allowed to use only the motion directions that do not disturb task 1.</p> </blockquote> <p>For multiple tasks, the process continues recursively.</p> <p>In software, nullspace projection often looks like a sequential algorithm:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">qddot</span> <span class="o">=</span> <span class="n">solveFirstTask</span><span class="p">(</span><span class="n">task1</span><span class="p">);</span>

<span class="n">N</span> <span class="o">=</span> <span class="n">computeNullspace</span><span class="p">(</span><span class="n">task1</span><span class="p">);</span>

<span class="n">qddot</span> <span class="o">+=</span> <span class="n">solveInNullspace</span><span class="p">(</span><span class="n">task2</span><span class="p">,</span> <span class="n">N</span><span class="p">);</span>

<span class="n">N</span> <span class="o">=</span> <span class="n">updateNullspace</span><span class="p">(</span><span class="n">N</span><span class="p">,</span> <span class="n">task2</span><span class="p">);</span>

<span class="n">qddot</span> <span class="o">+=</span> <span class="n">solveInNullspace</span><span class="p">(</span><span class="n">task3</span><span class="p">,</span> <span class="n">N</span><span class="p">);</span>
</code></pre></div></div> <h3 id="advantages-1">Advantages</h3> <p>The main advantage is strict task hierarchy.</p> <p>If implemented correctly, a lower-priority task cannot change the solution of a higher-priority task.</p> <p>This is very attractive for WBC.</p> <p>For example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>contact constraint
→ center of mass
→ hand task
→ posture
</code></pre></div></div> <p>The hand task should not break contact. The posture task should not disturb the hand task. Redundancy is used only after higher-priority objectives are satisfied.</p> <p>Nullspace projection also has a clear physical interpretation.</p> <p>It says:</p> <blockquote> <p>Use the remaining motion freedom after satisfying more important objectives.</p> </blockquote> <p>This matches the classical operational-space control view of task and posture decomposition.</p> <p>It can also be computationally efficient, especially when the task dimensions are moderate and the structure is exploited well.</p> <h3 id="limitations-1">Limitations</h3> <p>The main limitation is inequality constraints.</p> <p>Many real robot constraints are inequalities:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>normal force &gt;= 0
friction cone satisfied
tau_min &lt;= tau &lt;= tau_max
joint position within limits
contact wrench inside support region
</code></pre></div></div> <p>Nullspace projection handles equality tasks naturally. It is less natural for inequalities.</p> <p>This becomes a serious issue for dynamic legged robots.</p> <p>Contact is not simply:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>foot acceleration = 0
</code></pre></div></div> <p>It also requires feasible contact force.</p> <p>The foot cannot pull the ground. Tangential force must remain inside the friction cone. Reaction forces must stay within feasible bounds.</p> <p>If the controller treats contact only as an equality constraint, it may produce a mathematically consistent acceleration but a physically infeasible contact force.</p> <p>That is why pure nullspace projection can be difficult to use as the full solution for modern humanoid WBC.</p> <p>Another limitation is numerical robustness. Nullspace projectors depend on ranks, singular values, and generalized inverses. When a task becomes near-singular, the projector can become sensitive.</p> <h3 id="summary-1">Summary</h3> <p>Nullspace projection represents priority through subspace restriction.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Priority = allowed motion subspace
</code></pre></div></div> <p>It gives strict hierarchy and a clean interpretation.</p> <p>But inequality constraints are difficult to incorporate directly.</p> <hr/> <h2 id="4-hierarchical-qp-priority-as-optimization-hierarchy">4. Hierarchical QP: priority as optimization hierarchy</h2> <p>Hierarchical Quadratic Programming, or HQP, tries to combine two ideas:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>strict priority
+
optimization with constraints
</code></pre></div></div> <p>Instead of solving one weighted QP, HQP solves a sequence of optimization problems.</p> <p>At the first level, it solves the highest-priority task:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Level 1:
  minimize || J1 qddot - a1 ||²
  subject to constraints
</code></pre></div></div> <p>At the second level, it solves the next task while preserving the optimal result of level 1:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Level 2:
  minimize || J2 qddot - a2 ||²
  subject to
    level 1 optimality preserved
    constraints
</code></pre></div></div> <p>Then level 3 does the same for the next task.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Level 3:
  minimize || J3 qddot - a3 ||²
  subject to
    level 1 and 2 optimality preserved
    constraints
</code></pre></div></div> <p>In software, the structure may look like:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">level1</span><span class="p">.</span><span class="n">addTask</span><span class="p">(</span><span class="n">contact_task</span><span class="p">);</span>
<span class="n">level1</span><span class="p">.</span><span class="n">addConstraint</span><span class="p">(</span><span class="n">dynamics</span><span class="p">);</span>
<span class="n">level1</span><span class="p">.</span><span class="n">addConstraint</span><span class="p">(</span><span class="n">torque_limits</span><span class="p">);</span>
<span class="n">sol1</span> <span class="o">=</span> <span class="n">level1</span><span class="p">.</span><span class="n">solve</span><span class="p">();</span>

<span class="n">level2</span><span class="p">.</span><span class="n">addTask</span><span class="p">(</span><span class="n">com_task</span><span class="p">);</span>
<span class="n">level2</span><span class="p">.</span><span class="n">addConstraint</span><span class="p">(</span><span class="n">preserve</span><span class="p">(</span><span class="n">level1</span><span class="p">));</span>
<span class="n">sol2</span> <span class="o">=</span> <span class="n">level2</span><span class="p">.</span><span class="n">solve</span><span class="p">();</span>

<span class="n">level3</span><span class="p">.</span><span class="n">addTask</span><span class="p">(</span><span class="n">hand_task</span><span class="p">);</span>
<span class="n">level3</span><span class="p">.</span><span class="n">addConstraint</span><span class="p">(</span><span class="n">preserve</span><span class="p">(</span><span class="n">level2</span><span class="p">));</span>
<span class="n">sol3</span> <span class="o">=</span> <span class="n">level3</span><span class="p">.</span><span class="n">solve</span><span class="p">();</span>
</code></pre></div></div> <p>There are different ways to implement the preservation of previous optimality, often involving slack variables, equality constraints on previous optimal costs, or nullspace formulations.</p> <p>The important idea is:</p> <blockquote> <p>Lower-priority tasks are optimized only after higher-priority tasks have already claimed their solution quality.</p> </blockquote> <h3 id="advantages-2">Advantages</h3> <p>HQP gives a principled way to express strict priority.</p> <p>Unlike weighted QP, priority is not just a large number.</p> <p>A lower-priority task cannot improve itself by sacrificing a higher-priority task beyond the allowed relaxation.</p> <p>HQP can also include inequality constraints.</p> <p>This is a major advantage over pure nullspace projection.</p> <p>Torque limits, friction cones, contact force bounds, and joint limits can be represented as optimization constraints.</p> <p>So HQP is conceptually very suitable for humanoid WBC:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>strict task hierarchy
+
physical inequality constraints
+
optimization-based feasibility handling
</code></pre></div></div> <h3 id="limitations-2">Limitations</h3> <p>The main limitation is complexity.</p> <p>HQP is more difficult to implement than a single weighted QP.</p> <p>It may require multiple QP solves or a more sophisticated solver structure. It also needs careful treatment of infeasibility, slack variables, numerical tolerances, and rank conditions.</p> <p>This matters for real-time control.</p> <p>Humanoid WBC may need to run at hundreds of Hz or around 1 kHz. If each control tick requires several optimization problems, computation time becomes a serious design constraint.</p> <p>HQP also increases software complexity.</p> <p>The controller must manage:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>priority levels
task grouping
slack variables
constraint inheritance
optimality preservation
solver warm-starting
numerical tolerances
fallback behavior
</code></pre></div></div> <p>This is not impossible. Many humanoid systems use HQP-style methods successfully.</p> <p>But it is not a small abstraction.</p> <h3 id="summary-2">Summary</h3> <p>HQP represents priority through optimization levels.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Priority = optimization hierarchy
</code></pre></div></div> <p>It can provide strict hierarchy and handle inequalities.</p> <p>But it is more complex and can be computationally expensive.</p> <hr/> <h2 id="5-why-inequality-constraints-change-the-story">5. Why inequality constraints change the story</h2> <p>The difference between these methods becomes much sharper once contact constraints enter the problem.</p> <p>For a fixed-base manipulator doing only kinematic task tracking, nullspace projection can be very clean.</p> <p>But humanoids are not only tracking motions.</p> <p>They must satisfy contact and force feasibility.</p> <p>For a standing robot, contact constraints include at least:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>no slip
no pulling on the ground
contact wrench inside feasible region
normal force within reasonable bounds
torque limits respected
</code></pre></div></div> <p>These are not just nice-to-have conditions.</p> <p>They define whether the motion is physically possible.</p> <p>For example, suppose the controller computes a base or CoM acceleration that requires a contact force outside the friction cone.</p> <p>The task acceleration may look valid in kinematics.</p> <p>But the robot cannot realize it without slipping.</p> <p>This is where weighted QP and HQP have an advantage: inequality constraints fit naturally inside optimization.</p> <p>Nullspace projection can enforce motion constraints elegantly, but force inequalities require additional machinery.</p> <p>So the real distinction is not just mathematical style.</p> <p>It is about what the controller must guarantee.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>If the main issue is task redundancy:
  nullspace projection is attractive.

If the main issue is physical feasibility under inequalities:
  QP-based methods become attractive.

If both strict hierarchy and inequalities matter:
  HQP becomes attractive.
</code></pre></div></div> <p>This is why there is no universally best formulation.</p> <p>The right method depends on the robot, the task, the required control rate, and the kind of constraints that must be enforced.</p> <hr/> <h2 id="6-software-architecture-differences">6. Software architecture differences</h2> <p>These formulations lead to different code structures.</p> <h3 id="weighted-qp-architecture">Weighted QP architecture</h3> <p>In a weighted QP, tasks are usually added as costs:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">solver</span><span class="p">.</span><span class="n">addCost</span><span class="p">(</span><span class="n">com_task</span><span class="p">,</span> <span class="n">weight_com</span><span class="p">);</span>
<span class="n">solver</span><span class="p">.</span><span class="n">addCost</span><span class="p">(</span><span class="n">hand_task</span><span class="p">,</span> <span class="n">weight_hand</span><span class="p">);</span>
<span class="n">solver</span><span class="p">.</span><span class="n">addCost</span><span class="p">(</span><span class="n">posture_task</span><span class="p">,</span> <span class="n">weight_posture</span><span class="p">);</span>

<span class="n">solver</span><span class="p">.</span><span class="n">addConstraint</span><span class="p">(</span><span class="n">dynamics</span><span class="p">);</span>
<span class="n">solver</span><span class="p">.</span><span class="n">addConstraint</span><span class="p">(</span><span class="n">friction_cone</span><span class="p">);</span>
<span class="n">solver</span><span class="p">.</span><span class="n">addConstraint</span><span class="p">(</span><span class="n">torque_limit</span><span class="p">);</span>

<span class="n">solution</span> <span class="o">=</span> <span class="n">solver</span><span class="p">.</span><span class="n">solve</span><span class="p">();</span>
</code></pre></div></div> <p>The solver owns the tradeoff.</p> <p>The task object provides:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A matrix
target vector
weight
</code></pre></div></div> <p>The priority semantics are mostly encoded in weights.</p> <p>This is easy to implement, but harder to reason about when tasks conflict.</p> <hr/> <h3 id="nullspace-projection-architecture">Nullspace projection architecture</h3> <p>In nullspace projection, task execution is sequential:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">qddot</span> <span class="o">=</span> <span class="n">solveTask</span><span class="p">(</span><span class="n">task1</span><span class="p">);</span>
<span class="n">N</span> <span class="o">=</span> <span class="n">computeNullspace</span><span class="p">(</span><span class="n">task1</span><span class="p">);</span>

<span class="n">qddot</span> <span class="o">+=</span> <span class="n">solveTaskInNullspace</span><span class="p">(</span><span class="n">task2</span><span class="p">,</span> <span class="n">N</span><span class="p">);</span>
<span class="n">N</span> <span class="o">=</span> <span class="n">updateNullspace</span><span class="p">(</span><span class="n">N</span><span class="p">,</span> <span class="n">task2</span><span class="p">);</span>

<span class="n">qddot</span> <span class="o">+=</span> <span class="n">solveTaskInNullspace</span><span class="p">(</span><span class="n">task3</span><span class="p">,</span> <span class="n">N</span><span class="p">);</span>
</code></pre></div></div> <p>The controller owns the hierarchy.</p> <p>The task object provides:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Jacobian
desired acceleration
priority order
</code></pre></div></div> <p>The solver may be lightweight, but the nullspace logic becomes central.</p> <p>This is clean for strict equality-task hierarchy, but contact inequalities require additional handling.</p> <hr/> <h3 id="hqp-architecture">HQP architecture</h3> <p>In HQP, tasks are grouped by priority levels:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">hqp</span><span class="p">.</span><span class="n">level</span><span class="p">(</span><span class="mi">0</span><span class="p">).</span><span class="n">addTask</span><span class="p">(</span><span class="n">contact_or_balance</span><span class="p">);</span>
<span class="n">hqp</span><span class="p">.</span><span class="n">level</span><span class="p">(</span><span class="mi">0</span><span class="p">).</span><span class="n">addConstraint</span><span class="p">(</span><span class="n">dynamics</span><span class="p">);</span>
<span class="n">hqp</span><span class="p">.</span><span class="n">level</span><span class="p">(</span><span class="mi">0</span><span class="p">).</span><span class="n">addConstraint</span><span class="p">(</span><span class="n">torque_limits</span><span class="p">);</span>

<span class="n">hqp</span><span class="p">.</span><span class="n">level</span><span class="p">(</span><span class="mi">1</span><span class="p">).</span><span class="n">addTask</span><span class="p">(</span><span class="n">com_task</span><span class="p">);</span>
<span class="n">hqp</span><span class="p">.</span><span class="n">level</span><span class="p">(</span><span class="mi">1</span><span class="p">).</span><span class="n">preserveHigherPriority</span><span class="p">();</span>

<span class="n">hqp</span><span class="p">.</span><span class="n">level</span><span class="p">(</span><span class="mi">2</span><span class="p">).</span><span class="n">addTask</span><span class="p">(</span><span class="n">hand_task</span><span class="p">);</span>
<span class="n">hqp</span><span class="p">.</span><span class="n">level</span><span class="p">(</span><span class="mi">2</span><span class="p">).</span><span class="n">preserveHigherPriority</span><span class="p">();</span>

<span class="n">solution</span> <span class="o">=</span> <span class="n">hqp</span><span class="p">.</span><span class="n">solve</span><span class="p">();</span>
</code></pre></div></div> <p>The hierarchy is represented explicitly in the optimization structure.</p> <p>The task object provides:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Jacobian
desired acceleration
priority level
optional slack behavior
</code></pre></div></div> <p>The solver must manage priority preservation and constraints across levels.</p> <p>This is powerful, but the software stack becomes more involved.</p> <hr/> <h2 id="7-comparison-summary">7. Comparison summary</h2> <table> <thead> <tr> <th>Method</th> <th>Priority type</th> <th>Inequality constraints</th> <th>Main advantage</th> <th>Main limitation</th> </tr> </thead> <tbody> <tr> <td>Weighted QP</td> <td>Soft priority through weights</td> <td>Natural</td> <td>Simple and flexible</td> <td>No strict priority guarantee</td> </tr> <tr> <td>Nullspace Projection</td> <td>Strict priority through motion subspaces</td> <td>Difficult</td> <td>Clear hierarchy and efficient execution</td> <td>Hard to handle friction cones, torque limits, unilateral contacts directly</td> </tr> <tr> <td>HQP</td> <td>Strict priority through optimization levels</td> <td>Natural</td> <td>Strict hierarchy with constraints</td> <td>More complex and potentially expensive</td> </tr> </tbody> </table> <p>A shorter way to remember it:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Weighted QP:
  easy to formulate, hard to guarantee priority

Nullspace Projection:
  easy to interpret hierarchy, hard to handle inequalities

HQP:
  expressive and principled, harder to implement in real time
</code></pre></div></div> <hr/> <h2 id="8-which-one-should-a-wbc-codebase-support">8. Which one should a WBC codebase support?</h2> <p>For a software architecture project, the answer should not be hard-coded too early.</p> <p>Different controllers may need different priority mechanisms.</p> <p>A manipulation controller with soft objectives may be fine with a weighted QP.</p> <p>A classical operational-space controller may benefit from nullspace projection.</p> <p>A humanoid controller with strict contact, torque, and task hierarchy may need HQP or a related prioritized optimization method.</p> <p>So the architecture should separate:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Task representation
Priority representation
Constraint representation
Solver backend
Command adapter
</code></pre></div></div> <p>A <code class="language-plaintext highlighter-rouge">Task</code> should not assume whether it will be used inside a weighted QP, nullspace projection controller, or HQP solver.</p> <p>At minimum, a task should expose:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Jacobian
desired acceleration
task dimension
mask
weight
priority level
frame convention
</code></pre></div></div> <p>Then the solver backend can decide how to use it.</p> <p>For example:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">TaskSE3Equality</span> <span class="n">hand_task</span><span class="p">;</span>
<span class="n">TaskCoM</span> <span class="n">com_task</span><span class="p">;</span>
<span class="n">TaskPosture</span> <span class="n">posture_task</span><span class="p">;</span>

<span class="n">solver</span><span class="p">.</span><span class="n">addTask</span><span class="p">(</span><span class="n">com_task</span><span class="p">);</span>
<span class="n">solver</span><span class="p">.</span><span class="n">addTask</span><span class="p">(</span><span class="n">hand_task</span><span class="p">);</span>
<span class="n">solver</span><span class="p">.</span><span class="n">addTask</span><span class="p">(</span><span class="n">posture_task</span><span class="p">);</span>
</code></pre></div></div> <p>The same task objects could be interpreted differently:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Weighted QP:
  use task.weight()

Nullspace projection:
  use task.priority()

HQP:
  group tasks by priority level
</code></pre></div></div> <p>This is the software architecture point.</p> <p>The task should describe the control objective.</p> <p>The solver should decide how priority is enforced.</p> <hr/> <h2 id="9-summary">9. Summary</h2> <p>Task priority is one of the central problems in WBC.</p> <p>Once multiple tasks share the same robot body, conflicts are inevitable. The controller must decide which objectives are preserved and which objectives are allowed to degrade.</p> <p>Weighted QP, nullspace projection, and HQP answer this question differently.</p> <p>Weighted QP puts all tasks into one optimization and uses weights to shape the tradeoff.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>priority = cost weight
</code></pre></div></div> <p>Nullspace projection solves tasks sequentially and projects lower-priority tasks into the nullspace of higher-priority tasks.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>priority = allowed motion subspace
</code></pre></div></div> <p>HQP solves a sequence of optimization problems that preserve higher-priority optimality while optimizing lower-priority objectives.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>priority = optimization hierarchy
</code></pre></div></div> <p>None of these is universally best.</p> <p>Weighted QP is simple and handles inequalities naturally, but it does not provide strict hierarchy.</p> <p>Nullspace projection gives a clean strict hierarchy, but inequality constraints are difficult to incorporate directly.</p> <p>HQP can handle both hierarchy and inequalities, but it increases implementation and computational complexity.</p> <p>For real WBC software, the important decision is not only mathematical.</p> <p>It is architectural.</p> <p>A good WBC stack should separate task definition from priority enforcement. A task should define what it wants. The solver backend should decide how that task is combined with other tasks and constraints.</p> <p>In the next post, we will look at the <code class="language-plaintext highlighter-rouge">Task</code> abstraction more directly: what a task object should own, what it should expose, and how it should stay independent of the solver backend.</p> <h2 id="references">References</h2> <ul> <li> <p>Y. Nakamura, H. Hanafusa, and T. Yoshikawa, “Task-Priority Based Redundancy Control of Robot Manipulators,” <em>The International Journal of Robotics Research</em>, vol. 6, no. 2, pp. 3–15, 1987.</p> </li> <li> <p>A. Escande, N. Mansard, and P.-B. Wieber, “Hierarchical Quadratic Programming: Fast Online Humanoid-Robot Motion Generation,” <em>The International Journal of Robotics Research</em>, vol. 33, no. 7, pp. 1006–1028, 2014.</p> </li> <li> <p>D. Kim, J. Lee, J. Ahn, O. Campbell, H. Hwang, and L. Sentis, “Computationally-Robust and Efficient Prioritized Whole-Body Controller with Contact Constraints,” in <em>Proceedings of the IEEE/RSJ International Conference on Intelligent Robots and Systems (IROS)</em>, 2018.</p> </li> </ul>]]></content><author><name></name></author><category term="Whole Body Control"/><category term="Optimization"/><category term="Software"/><summary type="html"><![CDATA[A comparison of three common ways to handle task priority in Whole-Body Control: nullspace projection, weighted QP, and hierarchical QP.]]></summary></entry><entry xml:lang="en"><title type="html">[WBC 03] - From SE(3) Error to Task-Space PD Control</title><link href="https://dongho.dev/wbc03/" rel="alternate" type="text/html" title="[WBC 03] - From SE(3) Error to Task-Space PD Control"/><published>2026-06-15T00:00:00+00:00</published><updated>2026-06-15T00:00:00+00:00</updated><id>https://dongho.dev/wbc03</id><content type="html" xml:base="https://dongho.dev/wbc03/"><![CDATA[<h1 id="wbc03-from-se3-error-to-task-space-pd-control">WBC03. From SE(3) Error to Task-Space PD Control</h1> <p>In the previous post, we looked at how to represent floating-base robots in code.</p> <p>The main point was that configuration, velocity, acceleration, actuator torque, and contact force do not all live in the same space. For a floating-base robot, <code class="language-plaintext highlighter-rouge">q</code>, <code class="language-plaintext highlighter-rouge">v</code>, <code class="language-plaintext highlighter-rouge">qddot</code>, and <code class="language-plaintext highlighter-rouge">tau</code> have different meanings and often different dimensions.</p> <p>Now we move one layer up.</p> <p>Once the robot state is represented correctly, we need to define tasks.</p> <p>A common task is simple to describe in words:</p> <blockquote> <p>Move this frame to that desired pose.</p> </blockquote> <p>For example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Move the right hand frame to a target SE(3) pose.
</code></pre></div></div> <p>But this is not what the WBC solver directly receives.</p> <p>A WBC solver does not usually solve:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>frame_pose = desired_pose
</code></pre></div></div> <p>Instead, an acceleration-level WBC task converts pose and velocity errors into a desired task acceleration, then exposes that desired acceleration as a linear constraint on generalized acceleration.</p> <p>The final constraint usually looks like this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>J_task(q) qddot = a_des - drift
</code></pre></div></div> <p>This post explains how we get there.</p> <p>The core idea is:</p> <blockquote> <p>A pose task does not send a desired pose to the solver. It converts an SE(3) pose error into a tangent-space acceleration target.</p> </blockquote> <hr/> <h2 id="1-a-task-is-not-just-x_des">1. A task is not just <code class="language-plaintext highlighter-rouge">x_des</code></h2> <p>It is tempting to think of a task as a target.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">task</span><span class="p">.</span><span class="n">setTargetPose</span><span class="p">(</span><span class="n">x_des</span><span class="p">);</span>
</code></pre></div></div> <p>That is only the user-facing part.</p> <p>Internally, a WBC task must provide something the solver can use. For an acceleration-level inverse-dynamics solver, this usually means a constraint of the form:</p> \[J(q)\ddot{q} = a_{des} - a_{drift}\] <p>In code, this becomes something like:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">constraint</span><span class="p">.</span><span class="n">matrix</span><span class="p">()</span> <span class="o">=</span> <span class="n">J_task</span><span class="p">;</span>
<span class="n">constraint</span><span class="p">.</span><span class="n">vector</span><span class="p">()</span> <span class="o">=</span> <span class="n">a_des</span> <span class="o">-</span> <span class="n">drift</span><span class="p">;</span>
</code></pre></div></div> <p>The target pose itself is not enough.</p> <p>The task needs to compute:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>current pose
desired pose
pose error
current frame velocity
desired frame velocity
velocity error
desired task acceleration
Jacobian
drift acceleration
masked constraint rows
</code></pre></div></div> <p>So a task is better understood as a small controller.</p> <p>It reads the robot state, computes task-space error, applies feedback gains, and produces a solver constraint.</p> <p>A rough flow is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>desired pose
+ current pose
→ pose error

desired velocity
+ current velocity
→ velocity error

pose error
+ velocity error
+ Kp/Kd
→ desired task acceleration

desired task acceleration
+ Jacobian
+ drift
→ solver equality constraint
</code></pre></div></div> <p>In LaTeX:</p> \[e = x_{des} \ominus x\] \[e_v = v_{des} - v\] \[a_{des} = K_p e + K_d e_v + a_{ref}\] \[J(q)\ddot{q} = a_{des} - a_{drift}\] <p>In code, the same idea looks more like:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">poseError</span><span class="p">(</span><span class="n">current_pose</span><span class="p">,</span> <span class="n">desired_pose</span><span class="p">,</span> <span class="n">pose_error</span><span class="p">);</span>

<span class="n">velocity_error</span> <span class="o">=</span> <span class="n">desired_velocity_local</span> <span class="o">-</span> <span class="n">current_velocity_local</span><span class="p">;</span>

<span class="n">a_des</span> <span class="o">=</span>
  <span class="n">Kp</span><span class="p">.</span><span class="n">cwiseProduct</span><span class="p">(</span><span class="n">pose_error</span><span class="p">.</span><span class="n">toVector</span><span class="p">())</span> <span class="o">+</span>
  <span class="n">Kd</span><span class="p">.</span><span class="n">cwiseProduct</span><span class="p">(</span><span class="n">velocity_error</span><span class="p">.</span><span class="n">toVector</span><span class="p">())</span> <span class="o">+</span>
  <span class="n">desired_acceleration_local</span><span class="p">.</span><span class="n">toVector</span><span class="p">();</span>

<span class="n">constraint</span><span class="p">.</span><span class="n">matrix</span><span class="p">().</span><span class="n">row</span><span class="p">(</span><span class="n">idx</span><span class="p">)</span> <span class="o">=</span> <span class="n">J</span><span class="p">.</span><span class="n">row</span><span class="p">(</span><span class="n">i</span><span class="p">);</span>
<span class="n">constraint</span><span class="p">.</span><span class="n">vector</span><span class="p">().</span><span class="n">row</span><span class="p">(</span><span class="n">idx</span><span class="p">)</span> <span class="o">=</span> <span class="p">(</span><span class="n">a_des</span> <span class="o">-</span> <span class="n">drift</span><span class="p">).</span><span class="n">row</span><span class="p">(</span><span class="n">i</span><span class="p">);</span>
</code></pre></div></div> <p>This is the first important software boundary:</p> <blockquote> <p>The task owns error computation and desired acceleration. The solver only sees a linearized acceleration constraint.</p> </blockquote> <hr/> <h2 id="2-se3-pose-is-not-a-6d-vector">2. SE(3) pose is not a 6D vector</h2> <p>A 3D pose is usually represented as an element of <code class="language-plaintext highlighter-rouge">SE(3)</code>.</p> <p>In math:</p> \[T = \begin{bmatrix} R &amp; p \\ 0 &amp; 1 \end{bmatrix} \in SE(3)\] <p>where:</p> \[R \in SO(3), \qquad p \in \mathbb{R}^3\] <p>In code, Pinocchio represents this using <code class="language-plaintext highlighter-rouge">pinocchio::SE3</code>.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pinocchio</span><span class="o">::</span><span class="n">SE3</span> <span class="n">T</span><span class="p">;</span>

<span class="n">Eigen</span><span class="o">::</span><span class="n">Matrix3d</span> <span class="n">R</span> <span class="o">=</span> <span class="n">T</span><span class="p">.</span><span class="n">rotation</span><span class="p">();</span>
<span class="n">Eigen</span><span class="o">::</span><span class="n">Vector3d</span> <span class="n">p</span> <span class="o">=</span> <span class="n">T</span><span class="p">.</span><span class="n">translation</span><span class="p">();</span>
</code></pre></div></div> <p>This is the actual pose.</p> <p>It is not the same as a task error.</p> <p>That distinction matters.</p> <p>A pose is a transformation. An error is a tangent-space vector used by a controller.</p> <p>In other words:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SE(3):
  actual pose / transformation

se(3):
  local 6D motion / error / velocity-like quantity
</code></pre></div></div> <p>The task compares two <code class="language-plaintext highlighter-rouge">SE(3)</code> poses:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pinocchio</span><span class="o">::</span><span class="n">SE3</span> <span class="n">current</span><span class="p">;</span>
<span class="n">pinocchio</span><span class="o">::</span><span class="n">SE3</span> <span class="n">desired</span><span class="p">;</span>
</code></pre></div></div> <p>But the controller eventually wants a 6D vector:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pinocchio</span><span class="o">::</span><span class="n">Motion</span> <span class="n">error</span><span class="p">;</span>
<span class="n">Eigen</span><span class="o">::</span><span class="n">Matrix</span><span class="o">&lt;</span><span class="kt">double</span><span class="p">,</span> <span class="mi">6</span><span class="p">,</span> <span class="mi">1</span><span class="o">&gt;</span> <span class="n">error_vec</span> <span class="o">=</span> <span class="n">error</span><span class="p">.</span><span class="n">toVector</span><span class="p">();</span>
</code></pre></div></div> <p>This 6D vector is what can be multiplied by <code class="language-plaintext highlighter-rouge">Kp</code>.</p> <p>That is why the pose must be converted into an error first.</p> <hr/> <h2 id="3-relative-transform-comparing-current-and-desired-pose">3. Relative transform: comparing current and desired pose</h2> <p>Suppose the current frame pose is:</p> \[T_{current}\] <p>and the desired frame pose is:</p> \[T_{desired}\] <p>The task first computes the relative transform:</p> \[T_{err} = T_{current}^{-1} T_{desired}\] <p>In code:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">const</span> <span class="n">pinocchio</span><span class="o">::</span><span class="n">SE3</span> <span class="n">transform_error</span> <span class="o">=</span>
  <span class="n">current</span><span class="p">.</span><span class="n">actInv</span><span class="p">(</span><span class="n">desired</span><span class="p">);</span>
</code></pre></div></div> <p>This is equivalent to:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">transform_error</span> <span class="o">=</span> <span class="n">current</span><span class="p">.</span><span class="n">inverse</span><span class="p">()</span> <span class="o">*</span> <span class="n">desired</span><span class="p">;</span>
</code></pre></div></div> <p>Conceptually, <code class="language-plaintext highlighter-rouge">T_err</code> answers:</p> <blockquote> <p>From the current frame, where is the desired frame?</p> </blockquote> <p>If:</p> \[T_{err} = \begin{bmatrix} R_{err} &amp; p_{err} \\ 0 &amp; 1 \end{bmatrix}\] <p>then the task can extract translation and rotation error from this relative transform.</p> <p>The current implementation uses a split error:</p> \[e = \begin{bmatrix} p_{err} \\ \log(R_{err})^\vee \end{bmatrix}\] <p>In code:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">error</span><span class="p">.</span><span class="n">linear</span><span class="p">()</span> <span class="o">=</span> <span class="n">transform_error</span><span class="p">.</span><span class="n">translation</span><span class="p">();</span>
<span class="n">error</span><span class="p">.</span><span class="n">angular</span><span class="p">()</span> <span class="o">=</span> <span class="n">pinocchio</span><span class="o">::</span><span class="n">log3</span><span class="p">(</span><span class="n">transform_error</span><span class="p">.</span><span class="n">rotation</span><span class="p">());</span>

<span class="n">error_vec</span> <span class="o">=</span> <span class="n">error</span><span class="p">.</span><span class="n">toVector</span><span class="p">();</span>
</code></pre></div></div> <p>The side-by-side mapping is:</p> <table> <thead> <tr> <th>Math</th> <th>Code</th> </tr> </thead> <tbody> <tr> <td>$T_{err} = T_{current}^{-1}T_{desired}$</td> <td><code class="language-plaintext highlighter-rouge">current.actInv(desired)</code></td> </tr> <tr> <td>$p_{err}$</td> <td><code class="language-plaintext highlighter-rouge">transform_error.translation()</code></td> </tr> <tr> <td>$R_{err}$</td> <td><code class="language-plaintext highlighter-rouge">transform_error.rotation()</code></td> </tr> <tr> <td>$\log(R_{err})^\vee$</td> <td><code class="language-plaintext highlighter-rouge">pinocchio::log3(transform_error.rotation())</code></td> </tr> <tr> <td>$e = [p_{err}; \log(R_{err})^\vee]$</td> <td><code class="language-plaintext highlighter-rouge">error.toVector()</code></td> </tr> </tbody> </table> <p>This error is now a 6D vector that the controller can use.</p> <hr/> <h2 id="4-why-rotation-needs-log3">4. Why rotation needs <code class="language-plaintext highlighter-rouge">log3</code></h2> <p>Translation error is easy to understand.</p> <p>If the current position is:</p> \[p = \begin{bmatrix}1 \\ 0 \\ 0\end{bmatrix}\] <p>and the desired position is:</p> \[p_{des} = \begin{bmatrix}2 \\ 0 \\ 0\end{bmatrix}\] <p>then the position error is roughly:</p> \[e_p = \begin{bmatrix}1 \\ 0 \\ 0\end{bmatrix}\] <p>Rotation is different.</p> <p>A rotation matrix is not a normal vector. It is an element of <code class="language-plaintext highlighter-rouge">SO(3)</code>:</p> \[SO(3) = \left{ R \in \mathbb{R}^{3 \times 3} \mid R^\top R = I,\ \det(R)=1 \right}\] <p>So this is not a good orientation error:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Avoid</span>
<span class="n">Eigen</span><span class="o">::</span><span class="n">Matrix3d</span> <span class="n">R_error_bad</span> <span class="o">=</span> <span class="n">R_desired</span> <span class="o">-</span> <span class="n">R_current</span><span class="p">;</span>
</code></pre></div></div> <p>The result is just a matrix difference. It does not directly answer:</p> <blockquote> <p>Around which axis should I rotate, and by how much?</p> </blockquote> <p>For control, we want a 3D angular error vector.</p> <p>That vector lives in the Lie algebra <code class="language-plaintext highlighter-rouge">so(3)</code>.</p> <p>The logarithm map converts a relative rotation into this 3D angular error:</p> \[e_R = \log(R_{err})^\vee\] <p>In code:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Eigen</span><span class="o">::</span><span class="n">Vector3d</span> <span class="n">e_R</span> <span class="o">=</span>
  <span class="n">pinocchio</span><span class="o">::</span><span class="n">log3</span><span class="p">(</span><span class="n">R_error</span><span class="p">);</span>
</code></pre></div></div> <p>If the desired frame is rotated by a small angle around the z-axis, this error may look like:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[0, 0, 0.1]
</code></pre></div></div> <p>That is exactly the kind of vector a PD controller can use.</p> <p>So the pipeline is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SO(3) rotation error
→ log3
→ 3D angular error
→ Kp/Kd control
</code></pre></div></div> <p>This is why orientation error is not computed by subtracting quaternion coefficients or rotation matrices.</p> <hr/> <h2 id="5-split-error-versus-log6">5. Split error versus <code class="language-plaintext highlighter-rouge">log6</code></h2> <p>There are two common ways to create an SE(3) pose error.</p> <p>The current implementation uses a split error:</p> \[e_{split} = \begin{bmatrix} p_{err} \\ \log(R_{err})^\vee \end{bmatrix}\] <p>Code:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">const</span> <span class="n">pinocchio</span><span class="o">::</span><span class="n">SE3</span> <span class="n">transform_error</span> <span class="o">=</span>
  <span class="n">current</span><span class="p">.</span><span class="n">actInv</span><span class="p">(</span><span class="n">desired</span><span class="p">);</span>

<span class="n">error</span><span class="p">.</span><span class="n">linear</span><span class="p">()</span> <span class="o">=</span> <span class="n">transform_error</span><span class="p">.</span><span class="n">translation</span><span class="p">();</span>
<span class="n">error</span><span class="p">.</span><span class="n">angular</span><span class="p">()</span> <span class="o">=</span> <span class="n">pinocchio</span><span class="o">::</span><span class="n">log3</span><span class="p">(</span><span class="n">transform_error</span><span class="p">.</span><span class="n">rotation</span><span class="p">());</span>
</code></pre></div></div> <p>Another option is to use the full SE(3) logarithm:</p> \[e_{log6} = \log(T_{err})^\vee = \begin{bmatrix} \rho \\ \phi \end{bmatrix}\] <p>Code:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pinocchio</span><span class="o">::</span><span class="n">Motion</span> <span class="n">error</span> <span class="o">=</span>
  <span class="n">pinocchio</span><span class="o">::</span><span class="n">log6</span><span class="p">(</span><span class="n">transform_error</span><span class="p">);</span>
</code></pre></div></div> <p>Both produce a 6D vector.</p> <p>But they are not the same.</p> <p>The angular part is closely related:</p> \[\phi \approx \log(R_{err})^\vee\] <p>But the linear part is different.</p> <p>For the split error:</p> \[e_{split, linear} = p_{err}\] <p>For the full logarithm:</p> \[e_{log6, linear} = \rho\] <p>The value $\rho$ is not always equal to $p_{err}$. It is the linear component of the SE(3) logarithm, coupled with the rotational part of the transform.</p> <p>So changing from split error to <code class="language-plaintext highlighter-rouge">log6</code> is not a harmless refactor.</p> <p>It changes the meaning of the task error.</p> <table> <thead> <tr> <th>Method</th> <th>Math</th> <th>Code</th> <th>Linear part</th> </tr> </thead> <tbody> <tr> <td>Split error</td> <td>$[p_{err};\ \log(R_{err})^\vee]$</td> <td><code class="language-plaintext highlighter-rouge">translation() + log3(rotation())</code></td> <td>relative translation</td> </tr> <tr> <td>Full <code class="language-plaintext highlighter-rouge">log6</code></td> <td>$\log(T_{err})^\vee$</td> <td><code class="language-plaintext highlighter-rouge">pinocchio::log6(T_err)</code></td> <td>SE(3) logarithm linear component</td> </tr> </tbody> </table> <p>The split version has a practical advantage: position and orientation gains remain easy to interpret.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>linear gains:
  control relative translation

angular gains:
  control relative rotation
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">log6</code> version treats the whole pose error as one screw-like 6D motion. That can be mathematically cleaner for some formulations, but the linear part is less directly interpreted as “move x/y/z by this amount.”</p> <p>For a software stack, the important rule is:</p> <blockquote> <p>Pick one error definition intentionally. Do not switch between split error and <code class="language-plaintext highlighter-rouge">log6</code> without retuning gains and tests.</p> </blockquote> <hr/> <h2 id="6-se3-storage-vector-is-not-task-error">6. SE(3) storage vector is not task error</h2> <p>Another common source of confusion is that multiple things appear as <code class="language-plaintext highlighter-rouge">Eigen::Vector</code>.</p> <p>For example, a project may store an <code class="language-plaintext highlighter-rouge">SE(3)</code> reference pose as a 12D vector:</p> \[x_{storage} = \begin{bmatrix} p \\ \operatorname{vec}(R) \end{bmatrix} \in \mathbb{R}^{12}\] <p>This can be useful for storage, logging, messages, or trajectory interfaces.</p> <p>In code:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">se3ToVector</span><span class="p">(</span><span class="n">m_M_ref</span><span class="p">,</span> <span class="n">m_p_ref</span><span class="p">);</span>
<span class="n">vectorToSE3</span><span class="p">(</span><span class="n">ref</span><span class="p">.</span><span class="n">pos</span><span class="p">,</span> <span class="n">m_M_ref</span><span class="p">);</span>
</code></pre></div></div> <p>But this 12D vector is not a task error.</p> <p>It is only a storage representation.</p> <p>The actual task error is 6D:</p> \[e = \begin{bmatrix} e_{linear} \\ e_{angular} \end{bmatrix} \in \mathbb{R}^6\] <p>In code:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pinocchio</span><span class="o">::</span><span class="n">Motion</span> <span class="n">error</span><span class="p">;</span>

<span class="n">error</span><span class="p">.</span><span class="n">linear</span><span class="p">()</span> <span class="o">=</span> <span class="n">transform_error</span><span class="p">.</span><span class="n">translation</span><span class="p">();</span>
<span class="n">error</span><span class="p">.</span><span class="n">angular</span><span class="p">()</span> <span class="o">=</span> <span class="n">pinocchio</span><span class="o">::</span><span class="n">log3</span><span class="p">(</span><span class="n">transform_error</span><span class="p">.</span><span class="n">rotation</span><span class="p">());</span>

<span class="n">Eigen</span><span class="o">::</span><span class="n">Matrix</span><span class="o">&lt;</span><span class="kt">double</span><span class="p">,</span> <span class="mi">6</span><span class="p">,</span> <span class="mi">1</span><span class="o">&gt;</span> <span class="n">e</span> <span class="o">=</span> <span class="n">error</span><span class="p">.</span><span class="n">toVector</span><span class="p">();</span>
</code></pre></div></div> <p>A useful distinction is:</p> <table> <thead> <tr> <th>Quantity</th> <th style="text-align: right">Dimension</th> <th>Meaning</th> <th>Code</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">pinocchio::SE3</code></td> <td style="text-align: right">object</td> <td>actual pose transform</td> <td><code class="language-plaintext highlighter-rouge">oMi</code>, <code class="language-plaintext highlighter-rouge">m_M_ref</code></td> </tr> <tr> <td>SE(3) storage vector</td> <td style="text-align: right">12</td> <td>storage/display <code class="language-plaintext highlighter-rouge">[p; vec(R)]</code></td> <td><code class="language-plaintext highlighter-rouge">se3ToVector</code>, <code class="language-plaintext highlighter-rouge">vectorToSE3</code></td> </tr> <tr> <td>task error vector</td> <td style="text-align: right">6</td> <td>tangent-space error</td> <td><code class="language-plaintext highlighter-rouge">pinocchio::Motion::toVector()</code></td> </tr> <tr> <td>solver RHS</td> <td style="text-align: right">task dimension</td> <td>masked equality target</td> <td><code class="language-plaintext highlighter-rouge">constraint.vector()</code></td> </tr> </tbody> </table> <p>So even if both are represented as vectors in C++, they are not the same type of object.</p> <p>Flattened pose storage should not be subtracted to make a controller error.</p> <hr/> <h2 id="7-from-pose-error-to-desired-acceleration">7. From pose error to desired acceleration</h2> <p>Once we have a 6D pose error, the task can apply PD control.</p> <p>Let:</p> \[e_{pose} = \begin{bmatrix} e_p \\ e_R \end{bmatrix}\] <p>and let the velocity error be:</p> \[e_v = v_{ref} - v_{current}\] <p>Then the desired task acceleration is:</p> \[\begin{aligned} a_{des} &amp;= K_p \odot e_{pose} + K_d \odot e_v + a_{ref} \end{aligned}\] <p>Here, $\odot$ means element-wise multiplication.</p> <p>In code:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">m_a_des</span> <span class="o">=</span>
  <span class="n">m_Kp</span><span class="p">.</span><span class="n">cwiseProduct</span><span class="p">(</span><span class="n">m_p_error_vec</span><span class="p">)</span> <span class="o">+</span>
  <span class="n">m_Kd</span><span class="p">.</span><span class="n">cwiseProduct</span><span class="p">(</span><span class="n">m_v_error</span><span class="p">.</span><span class="n">toVector</span><span class="p">())</span> <span class="o">+</span>
  <span class="n">m_a_ref_local</span><span class="p">.</span><span class="n">toVector</span><span class="p">();</span>
</code></pre></div></div> <p>This is not just a random formula.</p> <p>It is the acceleration-level version of task-space PD control.</p> <p>The position/orientation error creates a restoring acceleration. The velocity error creates damping. The reference acceleration adds feedforward motion.</p> <p>The 6D ordering is usually:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[linear_x, linear_y, linear_z, angular_x, angular_y, angular_z]
</code></pre></div></div> <p>So the gains can be interpreted axis by axis:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kp[0:3]  -&gt; translational stiffness-like gains
Kp[3:6]  -&gt; rotational stiffness-like gains
Kd[0:3]  -&gt; translational damping-like gains
Kd[3:6]  -&gt; rotational damping-like gains
</code></pre></div></div> <p>This is one reason model-based task-space control remains useful.</p> <p>The gains have physical meaning.</p> <p>They are not perfect physical impedance unless the full dynamics shaping is implemented carefully, but they are still interpretable knobs.</p> <p>Increasing translational <code class="language-plaintext highlighter-rouge">Kp</code> makes the frame try harder to reduce position error. Increasing angular <code class="language-plaintext highlighter-rouge">Kd</code> damps rotational motion. Changing only the angular gains affects orientation behavior without changing translation behavior.</p> <p>That interpretability is one of the main benefits of writing the task explicitly.</p> <hr/> <h2 id="8-frame-convention-matters">8. Frame convention matters</h2> <p>The error, velocity, Jacobian, and desired acceleration must be expressed in compatible frames.</p> <p>A common choice is local frame control.</p> <p>In local frame mode, the task uses the frame-local Jacobian and expresses errors in the current frame convention.</p> <p>Code may look like:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">m_robot</span><span class="p">.</span><span class="n">frameJacobianLocal</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">m_frame_id</span><span class="p">,</span> <span class="n">m_J</span><span class="p">);</span>

<span class="n">poseError</span><span class="p">(</span><span class="n">oMi</span><span class="p">,</span> <span class="n">m_M_ref</span><span class="p">,</span> <span class="n">m_p_error</span><span class="p">);</span>

<span class="n">m_p_error_vec</span> <span class="o">=</span> <span class="n">m_p_error</span><span class="p">.</span><span class="n">toVector</span><span class="p">();</span>

<span class="n">m_v_error</span> <span class="o">=</span> <span class="n">m_wMl</span><span class="p">.</span><span class="n">actInv</span><span class="p">(</span><span class="n">m_v_ref</span><span class="p">)</span> <span class="o">-</span> <span class="n">v_frame</span><span class="p">;</span>
</code></pre></div></div> <p>The desired acceleration is then computed in the same local convention:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">m_a_des</span> <span class="o">=</span>
  <span class="n">m_Kp</span><span class="p">.</span><span class="n">cwiseProduct</span><span class="p">(</span><span class="n">m_p_error_vec</span><span class="p">)</span> <span class="o">+</span>
  <span class="n">m_Kd</span><span class="p">.</span><span class="n">cwiseProduct</span><span class="p">(</span><span class="n">m_v_error</span><span class="p">.</span><span class="n">toVector</span><span class="p">())</span> <span class="o">+</span>
  <span class="n">m_wMl</span><span class="p">.</span><span class="n">actInv</span><span class="p">(</span><span class="n">m_a_ref</span><span class="p">).</span><span class="n">toVector</span><span class="p">();</span>
</code></pre></div></div> <p>The important point is not the exact function names.</p> <p>The important point is consistency.</p> <p>If the Jacobian maps <code class="language-plaintext highlighter-rouge">qddot</code> to local-frame acceleration, then <code class="language-plaintext highlighter-rouge">a_des</code> should also be in that local-frame convention.</p> <p>Otherwise, the solver will satisfy the wrong equation.</p> <p>Some implementations also support a local world-oriented frame. In that case, the origin follows the task frame, but the axes are aligned with the world frame.</p> <p>Code may rotate the local quantities into a world-oriented representation:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">m_p_error_vec</span> <span class="o">=</span> <span class="n">m_wMl</span><span class="p">.</span><span class="n">toActionMatrix</span><span class="p">()</span> <span class="o">*</span> <span class="n">m_p_error</span><span class="p">.</span><span class="n">toVector</span><span class="p">();</span>
<span class="n">m_v_error</span> <span class="o">=</span> <span class="n">m_v_ref</span> <span class="o">-</span> <span class="n">m_wMl</span><span class="p">.</span><span class="n">act</span><span class="p">(</span><span class="n">v_frame</span><span class="p">);</span>
<span class="n">m_drift</span> <span class="o">=</span> <span class="n">m_wMl</span><span class="p">.</span><span class="n">act</span><span class="p">(</span><span class="n">m_drift</span><span class="p">);</span>
<span class="n">m_J</span> <span class="o">=</span> <span class="n">m_wMl</span><span class="p">.</span><span class="n">toActionMatrix</span><span class="p">()</span> <span class="o">*</span> <span class="n">m_J</span><span class="p">;</span>
</code></pre></div></div> <p>This changes how the task rows should be interpreted.</p> <p>The software rule is:</p> <blockquote> <p>Error, velocity, drift, and Jacobian rows must use the same frame convention.</p> </blockquote> <p>If they do not, the gains may appear unstable or directionally wrong even when the math looks correct.</p> <hr/> <h2 id="9-from-desired-acceleration-to-solver-constraint">9. From desired acceleration to solver constraint</h2> <p>The task-space acceleration relationship is:</p> \[a_{task} = J(q)\ddot{q} + a_{drift}\] <p>The drift term is the part of frame acceleration caused by the current robot velocity. It plays a role similar to:</p> \[\dot{J}(q, v)v\] <p>The task wants:</p> \[a_{task} = a_{des}\] <p>Substitute the acceleration relationship:</p> \[J(q)\ddot{q} + a_{drift} = a_{des}\] <p>Move the drift term to the right-hand side:</p> \[J(q)\ddot{q} = a_{des} - a_{drift}\] <p>This is the equality constraint that enters the HQP or QP solver.</p> <p>In code:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">constraint</span><span class="p">.</span><span class="n">matrix</span><span class="p">().</span><span class="n">row</span><span class="p">(</span><span class="n">idx</span><span class="p">)</span> <span class="o">=</span>
  <span class="n">J</span><span class="p">.</span><span class="n">row</span><span class="p">(</span><span class="n">i</span><span class="p">);</span>

<span class="n">constraint</span><span class="p">.</span><span class="n">vector</span><span class="p">().</span><span class="n">row</span><span class="p">(</span><span class="n">idx</span><span class="p">)</span> <span class="o">=</span>
  <span class="p">(</span><span class="n">a_des</span> <span class="o">-</span> <span class="n">drift</span><span class="p">).</span><span class="n">row</span><span class="p">(</span><span class="n">i</span><span class="p">);</span>
</code></pre></div></div> <p>With a mask, only selected rows are added.</p> <p>For example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[1, 1, 1, 0, 0, 0]  -&gt; position-only task
[0, 0, 0, 1, 1, 1]  -&gt; orientation-only task
[1, 1, 1, 1, 1, 1]  -&gt; full SE(3) task
</code></pre></div></div> <p>So the final task constraint is not necessarily 6D. It has as many rows as the active mask entries.</p> <p>The solver then finds <code class="language-plaintext highlighter-rouge">qddot</code> such that the active task rows are satisfied as well as possible, depending on the hierarchy and other constraints.</p> <p>A useful code-level check is the task acceleration reconstruction:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">a_task</span> <span class="o">=</span> <span class="n">constraint</span><span class="p">.</span><span class="n">matrix</span><span class="p">()</span> <span class="o">*</span> <span class="n">qddot_sol</span> <span class="o">+</span> <span class="n">drift_masked</span><span class="p">;</span>
</code></pre></div></div> <p>If the task is satisfied, this should match:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">a_des_masked</span>
</code></pre></div></div> <p>This is a good debugging tool.</p> <p>When a task behaves incorrectly, check these in order:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Is the pose error in the expected frame?
Is the velocity error in the same frame?
Is the Jacobian expressed in the same frame?
Is drift being subtracted on the RHS?
Is the mask selecting the intended rows?
Is the gain applied to the correct row ordering?
</code></pre></div></div> <p>Most bugs in SE(3) tasks are not solver bugs.</p> <p>They are convention bugs.</p> <hr/> <h2 id="10-latex-to-code-summary">10. LaTeX-to-code summary</h2> <p>The whole pipeline can be summarized as follows.</p> <table> <thead> <tr> <th>Step</th> <th>LaTeX</th> <th>Code</th> </tr> </thead> <tbody> <tr> <td>Current pose</td> <td>$T_{current}$</td> <td><code class="language-plaintext highlighter-rouge">oMi</code></td> </tr> <tr> <td>Desired pose</td> <td>$T_{desired}$</td> <td><code class="language-plaintext highlighter-rouge">m_M_ref</code></td> </tr> <tr> <td>Relative transform</td> <td>$T_{err}=T_{current}^{-1}T_{desired}$</td> <td><code class="language-plaintext highlighter-rouge">current.actInv(desired)</code></td> </tr> <tr> <td>Translation error</td> <td>$e_p=p_{err}$</td> <td><code class="language-plaintext highlighter-rouge">transform_error.translation()</code></td> </tr> <tr> <td>Rotation error</td> <td>$e_R=\log(R_{err})^\vee$</td> <td><code class="language-plaintext highlighter-rouge">pinocchio::log3(transform_error.rotation())</code></td> </tr> <tr> <td>Pose error</td> <td>$e=[e_p; e_R]$</td> <td><code class="language-plaintext highlighter-rouge">error.toVector()</code></td> </tr> <tr> <td>Velocity error</td> <td>$e_v=v_{ref}-v$</td> <td><code class="language-plaintext highlighter-rouge">m_v_error</code></td> </tr> <tr> <td>Desired acceleration</td> <td>$a_{des}=K_p\odot e+K_d\odot e_v+a_{ref}$</td> <td><code class="language-plaintext highlighter-rouge">m_a_des = Kp.cwiseProduct(...) + Kd.cwiseProduct(...) + a_ref</code></td> </tr> <tr> <td>Task acceleration</td> <td>$a_{task}=J(q)\ddot{q}+a_{drift}$</td> <td><code class="language-plaintext highlighter-rouge">J * qddot + drift</code></td> </tr> <tr> <td>Solver equality</td> <td>$J(q)\ddot{q}=a_{des}-a_{drift}$</td> <td><code class="language-plaintext highlighter-rouge">constraint.matrix() = J</code>, <code class="language-plaintext highlighter-rouge">constraint.vector() = a_des - drift</code></td> </tr> </tbody> </table> <p>The key transformation is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SE(3) pose target
→ relative transform
→ 6D tangent-space error
→ desired task acceleration
→ equality constraint on qddot
</code></pre></div></div> <p>That is what an SE(3) WBC task does.</p> <hr/> <h2 id="11-summary">11. Summary</h2> <p>An SE(3) task in WBC is not just a desired pose.</p> <p>It is a small acceleration-level controller.</p> <p>It compares the current frame pose and desired frame pose, builds a tangent-space pose error, applies task-space PD gains, and produces a linear equality constraint for the solver.</p> <p>The important distinction is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SE(3):
  pose / transform

se(3):
  local motion / error / velocity-like vector
</code></pre></div></div> <p>The current split-error implementation uses:</p> \[e = \begin{bmatrix} p_{err} \\ \log(R_{err})^\vee \end{bmatrix}\] <p>rather than:</p> \[e = \log(T_{err})^\vee\] <p>This is an intentional control-law choice. It keeps translation and rotation error meanings separate, which makes masks and gain tuning easier to interpret.</p> <p>After the pose error is built, task-space PD control creates:</p> \[\begin{aligned} a_{des} &amp;= K_p \odot e_{pose} + K_d \odot e_v + a_{ref} \end{aligned}\] <p>Finally, the task enters the WBC solver as:</p> \[J(q)\ddot{q} = a_{des} - a_{drift}\] <p>This is the key idea.</p> <p>The solver does not receive a pose.</p> <p>It receives an acceleration-level constraint.</p> <p>In the next post, we will look more carefully at what a <code class="language-plaintext highlighter-rouge">Task</code> object should own: target references, masks, gains, frame convention, Jacobian updates, drift terms, and constraint construction.</p>]]></content><author><name></name></author><category term="Whole Body Control"/><category term="Software"/><summary type="html"><![CDATA[How a pose target becomes a 6D task-space error, desired acceleration, and equality constraint in a WBC solver.]]></summary></entry><entry xml:lang="en"><title type="html">[WBC 02] - Representing Floating-Base Robots in Code</title><link href="https://dongho.dev/wbc02/" rel="alternate" type="text/html" title="[WBC 02] - Representing Floating-Base Robots in Code"/><published>2026-06-14T00:00:00+00:00</published><updated>2026-06-14T00:00:00+00:00</updated><id>https://dongho.dev/wbc02</id><content type="html" xml:base="https://dongho.dev/wbc02/"><![CDATA[<h1 id="wbc02-representing-floating-base-robots-in-code">WBC02. Representing Floating-Base Robots in Code</h1> <p>In the previous post, we looked at Whole-Body Control as a software architecture problem.</p> <p>WBC is not just a torque calculator. It is the layer that connects robot dynamics, contacts, task hierarchy, actuator limits, and hardware commands.</p> <p>Before building that layer, we need to answer a simpler question:</p> <blockquote> <p>How should a floating-base robot be represented in code?</p> </blockquote> <p>This sounds like an implementation detail.</p> <p>It is not.</p> <p>Many WBC bugs start from a wrong mental model of the robot state.</p> <p>For a fixed-base manipulator or a robot hand, it is often safe to think like this:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">q</span>     <span class="c1">// joint position</span>
<span class="n">qdot</span>  <span class="c1">// joint velocity</span>
<span class="n">tau</span>   <span class="c1">// joint torque</span>
</code></pre></div></div> <p>For an 8-DoF robot hand, this works.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nq</span> <span class="o">=</span> <span class="mi">8</span>
<span class="n">nv</span> <span class="o">=</span> <span class="mi">8</span>
<span class="n">na</span> <span class="o">=</span> <span class="mi">8</span>
</code></pre></div></div> <p>Configuration dimension, velocity dimension, and actuator dimension are all the same. The code can store <code class="language-plaintext highlighter-rouge">q</code>, <code class="language-plaintext highlighter-rouge">qdot</code>, and <code class="language-plaintext highlighter-rouge">tau</code> using the same joint ordering.</p> <p>But once a floating base enters the system, this simple picture breaks.</p> <hr/> <h2 id="1-fixed-base-robots-hide-the-problem">1. Fixed-base robots hide the problem</h2> <p>A fixed-base robot has its base rigidly attached to the world.</p> <p>A typical industrial manipulator bolted to a table is a fixed-base system. A robot hand attached to a wrist can also be treated as fixed-base if the wrist frame is considered fixed for the control problem.</p> <p>In this case, the robot motion is described only by the actuated joints:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">q</span> <span class="o">=</span> <span class="p">[</span><span class="n">joint_positions</span><span class="p">]</span>
<span class="n">v</span> <span class="o">=</span> <span class="p">[</span><span class="n">joint_velocities</span><span class="p">]</span>
</code></pre></div></div> <p>So a simple state structure can work:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="nc">RobotState</span> <span class="p">{</span>
  <span class="n">Vector</span> <span class="n">q</span><span class="p">;</span>
  <span class="n">Vector</span> <span class="n">qdot</span><span class="p">;</span>
  <span class="n">Vector</span> <span class="n">tau</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div> <p>The generalized coordinates are essentially joint coordinates. The generalized velocity is essentially joint velocity. The torque vector has the same dimension.</p> <p>This is why fixed-base control code can make the <code class="language-plaintext highlighter-rouge">q</code>, <code class="language-plaintext highlighter-rouge">qdot</code>, and <code class="language-plaintext highlighter-rouge">tau</code> story look deceptively simple.</p> <p>Floating-base robots do not have that luxury.</p> <p>A humanoid, quadruped, or floating hand-arm system has a base that moves in space. The robot state must include both the base motion and the actuated joint motion:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">q</span> <span class="o">=</span> <span class="p">[</span><span class="n">base_pose</span><span class="p">,</span> <span class="n">joint_positions</span><span class="p">]</span>
<span class="n">v</span> <span class="o">=</span> <span class="p">[</span><span class="n">base_velocity</span><span class="p">,</span> <span class="n">joint_velocities</span><span class="p">]</span>
</code></pre></div></div> <p>The floating base has 6-DoF rigid body motion:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>translation: 3 DoF
rotation:    3 DoF
total:       6 DoF
</code></pre></div></div> <p>Those six degrees of freedom are part of the robot dynamics.</p> <p>But they are not directly actuated.</p> <hr/> <h2 id="2-why-humanoids-are-underactuated">2. Why humanoids are underactuated</h2> <p>A natural question is:</p> <blockquote> <p>Humanoids usually have actuators at almost every joint. So why do we call them underactuated?</p> </blockquote> <p>The answer is that underactuation here is not about whether the robot has many joint motors.</p> <p>It is about whether every generalized degree of freedom in the dynamics can be directly actuated.</p> <p>A humanoid may have actuators at the hip, knee, ankle, shoulder, elbow, wrist, and neck. But the floating base itself is not directly actuated. There is no motor between the torso and the world that can directly command the base position or orientation.</p> <p>So the robot can be highly actuated at the joint level, while still being underactuated at the full-system level.</p> <p>For a humanoid with <code class="language-plaintext highlighter-rouge">na</code> actuated joints:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">v</span> <span class="o">=</span> <span class="p">[</span><span class="n">base_twist</span><span class="p">,</span> <span class="n">joint_velocities</span><span class="p">]</span>
</code></pre></div></div> <p>The generalized velocity dimension is:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nv</span> <span class="o">=</span> <span class="mi">6</span> <span class="o">+</span> <span class="n">na</span>
</code></pre></div></div> <p>But the actuator torque vector only contains joint torques:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">tau</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">=</span> <span class="n">na</span>
</code></pre></div></div> <p>The missing six dimensions are the floating-base degrees of freedom. They are part of the dynamics, but they are not directly commanded.</p> <p>This is why the actuation term has the structure:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Sᵀ tau = [0_base_6, tau_actuated]
</code></pre></div></div> <p>A compact floating-base dynamics equation is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>M(q) qddot + h(q, v) = Sᵀ tau + Jᵀ lambda
</code></pre></div></div> <p>Here, <code class="language-plaintext highlighter-rouge">Sᵀ tau</code> injects torque only into the actuated joint equations. The floating-base part receives no direct actuator torque.</p> <p>The base is controlled indirectly.</p> <p>For a standing humanoid, joint torques pass through the leg kinematic chains, create ground reaction forces at the feet, and those contact forces affect the motion of the floating base:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>joint torques
→ leg kinematic chains
→ foot contact forces
→ whole-body dynamics
→ floating-base motion
</code></pre></div></div> <p>This is why contacts are central in humanoid WBC. Without support contacts, the floating base cannot be controlled in the same way. With support contacts, the robot uses the environment as a physical pathway to regulate base motion, center of mass behavior, and balance.</p> <p>So the key point is:</p> <blockquote> <p>A humanoid is not underactuated because it lacks joint motors. It is underactuated because the floating base belongs to the dynamics but not to the actuator space.</p> </blockquote> <hr/> <h2 id="3-separate-nq-nv-and-na">3. Separate nq, nv, and na</h2> <p>The first rule of floating-base code is to separate <code class="language-plaintext highlighter-rouge">nq</code>, <code class="language-plaintext highlighter-rouge">nv</code>, and <code class="language-plaintext highlighter-rouge">na</code>.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nq</span> <span class="o">=</span> <span class="n">configuration</span> <span class="n">dimension</span>
<span class="n">nv</span> <span class="o">=</span> <span class="n">velocity</span> <span class="o">/</span> <span class="n">acceleration</span> <span class="n">dimension</span>
<span class="n">na</span> <span class="o">=</span> <span class="n">actuator</span> <span class="n">dimension</span>
</code></pre></div></div> <p>For fixed-base robots, these are often the same:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nq</span> <span class="o">=</span> <span class="n">nv</span> <span class="o">=</span> <span class="n">na</span>
</code></pre></div></div> <p>For floating-base robots, they are usually different.</p> <p>The main reason is orientation.</p> <p>A free-flyer base is often stored as position plus quaternion:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">base</span> <span class="n">configuration</span> <span class="o">=</span> <span class="n">position</span> <span class="mi">3</span> <span class="o">+</span> <span class="n">quaternion</span> <span class="mi">4</span> <span class="o">=</span> <span class="mi">7</span>
</code></pre></div></div> <p>But base velocity is represented as a 6D twist:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">base</span> <span class="n">velocity</span> <span class="o">=</span> <span class="n">linear</span> <span class="n">velocity</span> <span class="mi">3</span> <span class="o">+</span> <span class="n">angular</span> <span class="n">velocity</span> <span class="mi">3</span> <span class="o">=</span> <span class="mi">6</span>
</code></pre></div></div> <p>So for a floating-base robot with 8 actuated joints:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nq</span> <span class="o">=</span> <span class="mi">7</span> <span class="o">+</span> <span class="mi">8</span> <span class="o">=</span> <span class="mi">15</span>
<span class="n">nv</span> <span class="o">=</span> <span class="mi">6</span> <span class="o">+</span> <span class="mi">8</span> <span class="o">=</span> <span class="mi">14</span>
<span class="n">na</span> <span class="o">=</span> <span class="mi">8</span>
</code></pre></div></div> <p>The consistency checks are:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">q</span><span class="p">.</span><span class="n">size</span><span class="p">()</span>     <span class="o">==</span> <span class="n">nq</span>
<span class="n">v</span><span class="p">.</span><span class="n">size</span><span class="p">()</span>     <span class="o">==</span> <span class="n">nv</span>
<span class="n">qddot</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">==</span> <span class="n">nv</span>
<span class="n">tau</span><span class="p">.</span><span class="n">size</span><span class="p">()</span>   <span class="o">==</span> <span class="n">na</span>
</code></pre></div></div> <p>This is one of the first places floating-base code goes wrong.</p> <p>In fixed-base code, it is common to write <code class="language-plaintext highlighter-rouge">qdot</code> and assume that it has the same dimension as <code class="language-plaintext highlighter-rouge">q</code>.</p> <p>For floating-base robots, that assumption is dangerous.</p> <p>The vector <code class="language-plaintext highlighter-rouge">v</code> is not simply the Euclidean derivative of the stored configuration vector <code class="language-plaintext highlighter-rouge">q</code>. Quaternion orientation is stored using four numbers, but angular velocity is a 3D vector.</p> <p>That is why dynamics libraries such as Pinocchio use <code class="language-plaintext highlighter-rouge">v</code> for generalized velocity instead of treating velocity as raw <code class="language-plaintext highlighter-rouge">qdot</code>.</p> <p>This distinction affects:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>state storage
numerical integration
pose error computation
Jacobian dimensions
mass matrix dimensions
solver variable dimensions
command conversion
</code></pre></div></div> <p>In WBC software, this is not a naming detail. It is a structural requirement.</p> <hr/> <h2 id="4-quaternion-is-storage-lie-group-operations-are-computation">4. Quaternion is storage. Lie group operations are computation.</h2> <p>Floating-base orientation needs a representation.</p> <p>Common choices include:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Euler angles      3D, but singularity-prone
Quaternion        4D, with unit-norm constraint
Rotation matrix   9D, with orthogonality constraint
Lie algebra       3D, local representation
</code></pre></div></div> <p>A common question is:</p> <blockquote> <p>If we use Lie group operations, do we still need quaternions?</p> </blockquote> <p>Yes.</p> <p>They have different roles.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Quaternion:
  storage representation for orientation

Lie group operations:
  integration, difference, and local error computation

Twist:
  tangent-space velocity representation
</code></pre></div></div> <p>A quaternion is useful for storing global orientation. It avoids Euler-angle singularities and is more compact than a rotation matrix.</p> <p>But a quaternion should not be treated like an ordinary 4D vector during control.</p> <p>For example, this is usually wrong:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">orientation_error</span> <span class="o">=</span> <span class="n">quat_des</span><span class="p">.</span><span class="n">coeffs</span><span class="p">()</span> <span class="o">-</span> <span class="n">quat</span><span class="p">.</span><span class="n">coeffs</span><span class="p">();</span>
<span class="n">q_next</span> <span class="o">=</span> <span class="n">q</span> <span class="o">+</span> <span class="n">v</span> <span class="o">*</span> <span class="n">dt</span><span class="p">;</span>
</code></pre></div></div> <p>The first line computes an error in quaternion coefficient space, not a meaningful 3D orientation error.</p> <p>The second line treats the configuration as if it lived in a flat Euclidean vector space.</p> <p>For floating-base configurations, use manifold-aware operations:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">dq</span>     <span class="o">=</span> <span class="n">difference</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">q_current</span><span class="p">,</span> <span class="n">q_desired</span><span class="p">);</span>
<span class="n">q_next</span> <span class="o">=</span> <span class="n">integrate</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">q_current</span><span class="p">,</span> <span class="n">v</span> <span class="o">*</span> <span class="n">dt</span><span class="p">);</span>
</code></pre></div></div> <p>The key idea is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>q lives on the configuration manifold.
v lives in the tangent space.
</code></pre></div></div> <p>For a base orientation task, the error should be a 3D local angular error, not a 4D quaternion difference:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">R_err</span> <span class="o">=</span> <span class="n">R_des</span><span class="p">.</span><span class="n">inverse</span><span class="p">()</span> <span class="o">*</span> <span class="n">R_current</span><span class="p">;</span>
<span class="n">e_R</span>   <span class="o">=</span> <span class="n">log</span><span class="p">(</span><span class="n">R_err</span><span class="p">);</span>
</code></pre></div></div> <p>The convention may vary, but the principle is the same:</p> <blockquote> <p>Controller errors should live in the same tangent space as the velocities and accelerations used by the solver.</p> </blockquote> <hr/> <h2 id="5-keep-the-spaces-separate">5. Keep the spaces separate</h2> <p>Floating-base WBC operates across multiple spaces:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>configuration space:
  q ∈ R^nq

generalized velocity / acceleration space:
  v, qddot ∈ R^nv

actuator space:
  tau ∈ R^na

contact force space:
  lambda ∈ R^nc
</code></pre></div></div> <p>These spaces are related, but they are not interchangeable.</p> <p>For example, the solver may compute:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">qddot_sol</span>   <span class="c1">// size nv</span>
<span class="n">lambda_sol</span>  <span class="c1">// contact force dimension</span>
<span class="n">tau_sol</span>     <span class="c1">// size na</span>
</code></pre></div></div> <p>The acceleration solution includes both base acceleration and joint acceleration. But only the joint torque part can be sent to actuators.</p> <p>The base acceleration is not directly commanded. It is produced indirectly by actuator torques and contact forces.</p> <p>This also means contact is not optional in floating-base WBC. If the contact model is wrong, the base dynamics are wrong. If the contact force is infeasible, the desired motion is not physically realizable.</p> <p>A useful mental model is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>generalized dynamics space:
  where the equations are solved

actuator space:
  where motor commands exist

contact force space:
  where the robot interacts with the environment
</code></pre></div></div> <p>Confusing these spaces leads to one of the most common mistakes in floating-base control: treating the humanoid as if it were just a fixed-base manipulator with more joints.</p> <p>It is not.</p> <p>The base is not fixed. The base is not directly actuated. The base is controlled through the body and contacts.</p> <hr/> <h2 id="6-make-generalized-vector-packing-explicit">6. Make generalized vector packing explicit</h2> <p>The API can expose generalized vectors directly, or it can hide the packing behind semantic inputs. Both designs can work, but the boundary has to be clear.</p> <p>A low-level API may ask the caller to construct <code class="language-plaintext highlighter-rouge">q</code> and <code class="language-plaintext highlighter-rouge">v</code> directly:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">q</span><span class="p">.</span><span class="n">head</span><span class="o">&lt;</span><span class="mi">7</span><span class="o">&gt;</span><span class="p">()</span> <span class="o">=</span> <span class="n">base_pose_as_xyz_quaternion</span><span class="p">;</span>
<span class="n">q</span><span class="p">.</span><span class="n">tail</span><span class="p">(</span><span class="n">na</span><span class="p">)</span>  <span class="o">=</span> <span class="n">joint_position</span><span class="p">;</span>

<span class="n">v</span><span class="p">.</span><span class="n">head</span><span class="o">&lt;</span><span class="mi">6</span><span class="o">&gt;</span><span class="p">()</span> <span class="o">=</span> <span class="n">base_twist</span><span class="p">;</span>
<span class="n">v</span><span class="p">.</span><span class="n">tail</span><span class="p">(</span><span class="n">na</span><span class="p">)</span>  <span class="o">=</span> <span class="n">joint_velocity</span><span class="p">;</span>
</code></pre></div></div> <p>In that case, the contract must be explicit:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Is the base pose first or last?
Is quaternion ordering xyzw or wxyz?
Is the base velocity spatial or body-frame?
Is qdot the same thing as v?
Is tau dimension nq, nv, or na?
</code></pre></div></div> <p>For a higher-level controller API, another option is to accept semantic state components and let <code class="language-plaintext highlighter-rouge">RobotSystem</code> pack the generalized vectors internally.</p> <p>For example:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="nc">FloatingBaseState</span> <span class="p">{</span>
  <span class="n">pinocchio</span><span class="o">::</span><span class="n">SE3</span> <span class="n">pose_world_base</span><span class="p">;</span>
  <span class="n">pinocchio</span><span class="o">::</span><span class="n">Motion</span> <span class="n">twist_world_base</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div> <p>Then the public API can separate fixed-base and floating-base updates:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">updateState</span><span class="p">(</span><span class="n">Vector</span> <span class="n">q_actuated</span><span class="p">,</span>
                 <span class="n">Vector</span> <span class="n">qdot_actuated</span><span class="p">,</span>
                 <span class="n">Vector</span> <span class="n">tau</span><span class="p">);</span>

<span class="kt">void</span> <span class="nf">updateState</span><span class="p">(</span><span class="n">Vector</span> <span class="n">q_actuated</span><span class="p">,</span>
                 <span class="n">Vector</span> <span class="n">qdot_actuated</span><span class="p">,</span>
                 <span class="n">Vector</span> <span class="n">tau</span><span class="p">,</span>
                 <span class="k">const</span> <span class="n">FloatingBaseState</span><span class="o">&amp;</span> <span class="n">floating_base</span><span class="p">);</span>
</code></pre></div></div> <p>For a fixed-base robot:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">robot</span><span class="p">.</span><span class="n">updateState</span><span class="p">(</span><span class="n">q_actuated</span><span class="p">,</span> <span class="n">qdot_actuated</span><span class="p">,</span> <span class="n">tau</span><span class="p">);</span>
</code></pre></div></div> <p>For a floating-base robot:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">robot</span><span class="p">.</span><span class="n">updateState</span><span class="p">(</span><span class="n">q_actuated</span><span class="p">,</span> <span class="n">qdot_actuated</span><span class="p">,</span> <span class="n">tau</span><span class="p">,</span> <span class="n">floating_base</span><span class="p">);</span>
</code></pre></div></div> <p>In the semantic-input version, <code class="language-plaintext highlighter-rouge">RobotSystem</code> constructs <code class="language-plaintext highlighter-rouge">q</code> and <code class="language-plaintext highlighter-rouge">v</code> using the dynamics library convention:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// fixed-base</span>
<span class="n">q</span> <span class="o">=</span> <span class="n">q_actuated</span><span class="p">;</span>
<span class="n">v</span> <span class="o">=</span> <span class="n">qdot_actuated</span><span class="p">;</span>

<span class="c1">// floating-base</span>
<span class="n">q</span> <span class="o">=</span> <span class="p">[</span><span class="n">base_position</span><span class="p">,</span> <span class="n">base_quaternion</span><span class="p">,</span> <span class="n">q_actuated</span><span class="p">];</span>
<span class="n">v</span> <span class="o">=</span> <span class="p">[</span><span class="n">base_twist</span><span class="p">,</span> <span class="n">qdot_actuated</span><span class="p">];</span>
</code></pre></div></div> <p>The responsibility split is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User:
  provides joint state and floating-base state semantically.

RobotSystem:
  converts semantic state into q and v.

Controller:
  uses q, v, dynamics terms, tasks, and contacts.
</code></pre></div></div> <p>Internally, the canonical state should match the dynamics library:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="nc">RobotState</span> <span class="p">{</span>
  <span class="n">Vector</span> <span class="n">q</span><span class="p">;</span>    <span class="c1">// size nq</span>
  <span class="n">Vector</span> <span class="n">v</span><span class="p">;</span>    <span class="c1">// size nv</span>
  <span class="n">Vector</span> <span class="n">tau</span><span class="p">;</span>  <span class="c1">// size na</span>
<span class="p">};</span>
</code></pre></div></div> <p>This makes the dynamics code clear:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">M</span><span class="p">(</span><span class="n">q</span><span class="p">)</span>        <span class="c1">// nv x nv</span>
<span class="n">h</span><span class="p">(</span><span class="n">q</span><span class="p">,</span> <span class="n">v</span><span class="p">)</span>     <span class="c1">// nv</span>
<span class="n">qddot</span>       <span class="c1">// nv</span>
<span class="n">tau</span>         <span class="c1">// na</span>
</code></pre></div></div> <p>The WBC solver usually operates in <code class="language-plaintext highlighter-rouge">nv</code> space:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">decision</span> <span class="n">variable</span> <span class="o">=</span> <span class="p">[</span><span class="n">qddot</span><span class="p">,</span> <span class="n">lambda</span><span class="p">]</span>

<span class="n">qddot</span> <span class="err">∈</span> <span class="n">R</span><span class="o">^</span><span class="n">nv</span>
<span class="n">lambda</span> <span class="err">∈</span> <span class="n">R</span><span class="o">^</span><span class="n">contact_force_dim</span>
</code></pre></div></div> <p>So this is wrong for floating-base systems:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">qddot</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">==</span> <span class="n">q</span><span class="p">.</span><span class="n">size</span><span class="p">()</span>
</code></pre></div></div> <p>The correct check is:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">qddot</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">==</span> <span class="n">nv</span>
</code></pre></div></div> <p>Dimension checks are not optional housekeeping. In WBC code, they are part of the safety structure.</p> <hr/> <h2 id="7-solver-results-are-not-actuator-commands">7. Solver results are not actuator commands</h2> <p>Another common source of confusion is the relationship between solver outputs and hardware commands.</p> <p>An inverse-dynamics WBC solver may produce:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">qddot_sol</span>
<span class="n">lambda_sol</span>
<span class="n">tau_sol</span>
</code></pre></div></div> <p>But the hardware interface may expect:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="nc">RobotCommand</span> <span class="p">{</span>
  <span class="n">Vector</span> <span class="n">q</span><span class="p">;</span>
  <span class="n">Vector</span> <span class="n">qdot</span><span class="p">;</span>
  <span class="n">Vector</span> <span class="n">tau</span><span class="p">;</span>
  <span class="n">Vector</span> <span class="n">kp</span><span class="p">;</span>
  <span class="n">Vector</span> <span class="n">kd</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div> <p>These command fields are not the same as solver variables.</p> <p>For example, <code class="language-plaintext highlighter-rouge">qddot_sol</code> may need to be integrated:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">qdot_cmd</span> <span class="o">=</span> <span class="n">qdot_current</span> <span class="o">+</span> <span class="n">qddot_sol</span> <span class="o">*</span> <span class="n">dt</span><span class="p">;</span>
<span class="n">q_cmd</span>    <span class="o">=</span> <span class="n">integrate</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">q_current</span><span class="p">,</span> <span class="n">qdot_cmd</span> <span class="o">*</span> <span class="n">dt</span><span class="p">);</span>
</code></pre></div></div> <p>Then the command adapter may apply:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>velocity clamp
position clamp
torque clamp
rate limit
driver-specific scaling
</code></pre></div></div> <p>The solver operates in dynamics-level quantities:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>qddot_sol
lambda_sol
tau_sol
</code></pre></div></div> <p>The command adapter produces hardware-level quantities:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>q_cmd
qdot_cmd
tau_ff_cmd
kp_cmd
kd_cmd
</code></pre></div></div> <p>Mixing these layers creates ambiguity.</p> <p>If a variable is named <code class="language-plaintext highlighter-rouge">q_cmd</code>, it should mean the command being sent toward the hardware interface. It should not mean desired task position, raw solver output, or unclamped integrated state.</p> <p>A useful convention is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>_des:
  desired value from planner, task, or reference generator

_sol:
  solver result

_cmd:
  command sent toward the hardware interface
</code></pre></div></div> <p>The exact naming convention can vary, but the semantic distinction should exist.</p> <p>Solver results are not automatically actuator commands.</p> <hr/> <h2 id="8-dimensions-show-up-in-matrix-multiplication">8. Dimensions show up in matrix multiplication</h2> <p>The easiest way to see why these distinctions matter is to write the matrix products that the controller actually evaluates.</p> <p>For a floating-base inverse dynamics problem, the equation has the form:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>M(q) qddot + h(q, v) = S^T tau + J_c(q)^T lambda
</code></pre></div></div> <p>The dimensions are:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>q      in R^nq
v      in R^nv
qddot  in R^nv
tau    in R^na
lambda in R^nc

M      in R^(nv x nv)
h      in R^nv
S      in R^(na x nv)
J_c    in R^(nc x nv)
</code></pre></div></div> <p>So the multiplication is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(nv x nv)(nv) + (nv)
  = (nv x na)(na) + (nv x nc)(nc)
</code></pre></div></div> <p>This one line already tells us several implementation details. <code class="language-plaintext highlighter-rouge">qddot</code> must live in <code class="language-plaintext highlighter-rouge">nv</code> space. <code class="language-plaintext highlighter-rouge">tau</code> must live in actuator space. A contact Jacobian has <code class="language-plaintext highlighter-rouge">nv</code> columns because it maps generalized velocity to contact velocity. <code class="language-plaintext highlighter-rouge">S^T</code> maps actuator torque into generalized force space.</p> <p>With Eigen, these dimensions become the row and column sizes of <code class="language-plaintext highlighter-rouge">Matrix</code> and <code class="language-plaintext highlighter-rouge">Vector</code>.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">using</span> <span class="n">Vector</span> <span class="o">=</span> <span class="n">Eigen</span><span class="o">::</span><span class="n">VectorXd</span><span class="p">;</span>
<span class="k">using</span> <span class="n">Matrix</span> <span class="o">=</span> <span class="n">Eigen</span><span class="o">::</span><span class="n">MatrixXd</span><span class="p">;</span>

<span class="k">const</span> <span class="kt">int</span> <span class="n">nq</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="n">nq</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">int</span> <span class="n">nv</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="n">nv</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">int</span> <span class="n">na</span> <span class="o">=</span> <span class="n">robot</span><span class="p">.</span><span class="n">numActuatedDofs</span><span class="p">();</span>
<span class="k">const</span> <span class="kt">int</span> <span class="n">nc</span> <span class="o">=</span> <span class="n">contact</span><span class="p">.</span><span class="n">forceDim</span><span class="p">();</span>

<span class="n">Vector</span> <span class="nf">q</span><span class="p">(</span><span class="n">nq</span><span class="p">);</span>
<span class="n">Vector</span> <span class="nf">v</span><span class="p">(</span><span class="n">nv</span><span class="p">);</span>
<span class="n">Vector</span> <span class="nf">qddot</span><span class="p">(</span><span class="n">nv</span><span class="p">);</span>
<span class="n">Vector</span> <span class="nf">tau</span><span class="p">(</span><span class="n">na</span><span class="p">);</span>
<span class="n">Vector</span> <span class="nf">lambda</span><span class="p">(</span><span class="n">nc</span><span class="p">);</span>

<span class="n">Matrix</span> <span class="nf">M</span><span class="p">(</span><span class="n">nv</span><span class="p">,</span> <span class="n">nv</span><span class="p">);</span>
<span class="n">Vector</span> <span class="nf">h</span><span class="p">(</span><span class="n">nv</span><span class="p">);</span>
<span class="n">Matrix</span> <span class="nf">S</span><span class="p">(</span><span class="n">na</span><span class="p">,</span> <span class="n">nv</span><span class="p">);</span>
<span class="n">Matrix</span> <span class="nf">Jc</span><span class="p">(</span><span class="n">nc</span><span class="p">,</span> <span class="n">nv</span><span class="p">);</span>
</code></pre></div></div> <p>Then Eigen computes each product by matching the inner dimension:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Vector</span> <span class="nf">generalized_inertia</span><span class="p">(</span><span class="n">nv</span><span class="p">);</span>
<span class="n">generalized_inertia</span><span class="p">.</span><span class="n">noalias</span><span class="p">()</span> <span class="o">=</span> <span class="n">M</span> <span class="o">*</span> <span class="n">qddot</span><span class="p">;</span>
<span class="c1">// (nv x nv) * (nv x 1) -&gt; (nv x 1)</span>

<span class="n">Vector</span> <span class="nf">generalized_actuation</span><span class="p">(</span><span class="n">nv</span><span class="p">);</span>
<span class="n">generalized_actuation</span><span class="p">.</span><span class="n">noalias</span><span class="p">()</span> <span class="o">=</span> <span class="n">S</span><span class="p">.</span><span class="n">transpose</span><span class="p">()</span> <span class="o">*</span> <span class="n">tau</span><span class="p">;</span>
<span class="c1">// (nv x na) * (na x 1) -&gt; (nv x 1)</span>

<span class="n">Vector</span> <span class="nf">generalized_contact</span><span class="p">(</span><span class="n">nv</span><span class="p">);</span>
<span class="n">generalized_contact</span><span class="p">.</span><span class="n">noalias</span><span class="p">()</span> <span class="o">=</span> <span class="n">Jc</span><span class="p">.</span><span class="n">transpose</span><span class="p">()</span> <span class="o">*</span> <span class="n">lambda</span><span class="p">;</span>
<span class="c1">// (nv x nc) * (nc x 1) -&gt; (nv x 1)</span>

<span class="n">Vector</span> <span class="n">residual</span> <span class="o">=</span>
    <span class="n">generalized_inertia</span> <span class="o">+</span> <span class="n">h</span>
  <span class="o">-</span> <span class="n">generalized_actuation</span>
  <span class="o">-</span> <span class="n">generalized_contact</span><span class="p">;</span>
</code></pre></div></div> <p>The staged version is intentionally verbose. The same computation could be written in one expression:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Vector</span> <span class="n">residual</span> <span class="o">=</span>
    <span class="n">M</span> <span class="o">*</span> <span class="n">qddot</span> <span class="o">+</span> <span class="n">h</span>
  <span class="o">-</span> <span class="n">S</span><span class="p">.</span><span class="n">transpose</span><span class="p">()</span> <span class="o">*</span> <span class="n">tau</span>
  <span class="o">-</span> <span class="n">Jc</span><span class="p">.</span><span class="n">transpose</span><span class="p">()</span> <span class="o">*</span> <span class="n">lambda</span><span class="p">;</span>
</code></pre></div></div> <p>Eigen represents the right-hand side as an expression and evaluates it when assigning into <code class="language-plaintext highlighter-rouge">residual</code>. Breaking the expression into named terms makes the vector spaces easier to inspect, and <code class="language-plaintext highlighter-rouge">noalias()</code> tells Eigen that the destination does not overlap with the product inputs.</p> <p>This is not just notation. Eigen checks matrix product compatibility through the product dimensions.</p> <p>For example:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Vector</span> <span class="nf">tau_wrong</span><span class="p">(</span><span class="n">nv</span><span class="p">);</span>

<span class="n">Vector</span> <span class="n">generalized_actuation</span> <span class="o">=</span>
    <span class="n">S</span><span class="p">.</span><span class="n">transpose</span><span class="p">()</span> <span class="o">*</span> <span class="n">tau_wrong</span><span class="p">;</span>
<span class="c1">// (nv x na) * (nv x 1)</span>
<span class="c1">// invalid unless na == nv</span>
</code></pre></div></div> <p>With dynamic-size Eigen types such as <code class="language-plaintext highlighter-rouge">MatrixXd</code> and <code class="language-plaintext highlighter-rouge">VectorXd</code>, this is checked at runtime in debug builds. With fixed-size Eigen types, incompatible products can be rejected at compile time. Most robot models use dynamic sizes, so it is still useful to add explicit API checks:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">assert</span><span class="p">(</span><span class="n">q</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">==</span> <span class="n">nq</span><span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="n">v</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">==</span> <span class="n">nv</span><span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="n">qddot</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">==</span> <span class="n">nv</span><span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="n">tau</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">==</span> <span class="n">na</span><span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="n">lambda</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">==</span> <span class="n">nc</span><span class="p">);</span>

<span class="n">assert</span><span class="p">(</span><span class="n">M</span><span class="p">.</span><span class="n">rows</span><span class="p">()</span> <span class="o">==</span> <span class="n">nv</span> <span class="o">&amp;&amp;</span> <span class="n">M</span><span class="p">.</span><span class="n">cols</span><span class="p">()</span> <span class="o">==</span> <span class="n">nv</span><span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="n">S</span><span class="p">.</span><span class="n">rows</span><span class="p">()</span> <span class="o">==</span> <span class="n">na</span> <span class="o">&amp;&amp;</span> <span class="n">S</span><span class="p">.</span><span class="n">cols</span><span class="p">()</span> <span class="o">==</span> <span class="n">nv</span><span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="n">Jc</span><span class="p">.</span><span class="n">rows</span><span class="p">()</span> <span class="o">==</span> <span class="n">nc</span> <span class="o">&amp;&amp;</span> <span class="n">Jc</span><span class="p">.</span><span class="n">cols</span><span class="p">()</span> <span class="o">==</span> <span class="n">nv</span><span class="p">);</span>
</code></pre></div></div> <p>If <code class="language-plaintext highlighter-rouge">tau</code> is accidentally allocated with size <code class="language-plaintext highlighter-rouge">nv</code>, this product should fail a dimension check for a floating-base robot where <code class="language-plaintext highlighter-rouge">nv != na</code>. If <code class="language-plaintext highlighter-rouge">qddot</code> is allocated with <code class="language-plaintext highlighter-rouge">q.size()</code>, the bug may only appear when the robot becomes floating-base, because <code class="language-plaintext highlighter-rouge">nq</code> and <code class="language-plaintext highlighter-rouge">nv</code> are no longer the same.</p> <p>Task equations have the same structure. A task Jacobian maps generalized velocity to task velocity:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>xdot = J_task(q) v
</code></pre></div></div> <p>For acceleration-level WBC, the task objective usually contains:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>J_task(q) qddot + Jdot_v = xddot_cmd
</code></pre></div></div> <p>The dimensions are:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(task_dim x nv)(nv) + (task_dim) = (task_dim)
</code></pre></div></div> <p>In code:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Matrix</span> <span class="nf">Jtask</span><span class="p">(</span><span class="n">task_dim</span><span class="p">,</span> <span class="n">nv</span><span class="p">);</span>
<span class="n">Vector</span> <span class="nf">Jdot_v</span><span class="p">(</span><span class="n">task_dim</span><span class="p">);</span>
<span class="n">Vector</span> <span class="nf">xddot_cmd</span><span class="p">(</span><span class="n">task_dim</span><span class="p">);</span>

<span class="n">Vector</span> <span class="n">task_error</span> <span class="o">=</span> <span class="n">Jtask</span> <span class="o">*</span> <span class="n">qddot</span> <span class="o">+</span> <span class="n">Jdot_v</span> <span class="o">-</span> <span class="n">xddot_cmd</span><span class="p">;</span>
</code></pre></div></div> <p>Again, the matrix multiplication makes the space explicit. The task does not multiply by <code class="language-plaintext highlighter-rouge">qdot</code> with size <code class="language-plaintext highlighter-rouge">nq</code>; it multiplies by generalized velocity or acceleration in tangent space with size <code class="language-plaintext highlighter-rouge">nv</code>.</p> <p>This is also why floating-base integration should use the dynamics library operation instead of raw vector addition:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Vector</span> <span class="n">v_next</span> <span class="o">=</span> <span class="n">v</span> <span class="o">+</span> <span class="n">qddot_sol</span> <span class="o">*</span> <span class="n">dt</span><span class="p">;</span>
<span class="n">Vector</span> <span class="n">q_next</span> <span class="o">=</span> <span class="n">pinocchio</span><span class="o">::</span><span class="n">integrate</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">q</span><span class="p">,</span> <span class="n">v_next</span> <span class="o">*</span> <span class="n">dt</span><span class="p">);</span>
</code></pre></div></div> <p>The solver result is still not automatically the hardware command. A command adapter may extract the actuated part and apply command policy:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="nc">IDSolution</span> <span class="p">{</span>
  <span class="n">Vector</span> <span class="n">qddot_sol</span><span class="p">;</span>   <span class="c1">// nv</span>
  <span class="n">Vector</span> <span class="n">lambda_sol</span><span class="p">;</span>  <span class="c1">// nc</span>
  <span class="n">Vector</span> <span class="n">tau_sol</span><span class="p">;</span>     <span class="c1">// na</span>
<span class="p">};</span>

<span class="k">struct</span> <span class="nc">RobotCommand</span> <span class="p">{</span>
  <span class="n">Vector</span> <span class="n">q</span><span class="p">;</span>     <span class="c1">// na</span>
  <span class="n">Vector</span> <span class="n">qdot</span><span class="p">;</span>  <span class="c1">// na</span>
  <span class="n">Vector</span> <span class="n">tau</span><span class="p">;</span>   <span class="c1">// na</span>
  <span class="n">Vector</span> <span class="n">kp</span><span class="p">;</span>    <span class="c1">// na</span>
  <span class="n">Vector</span> <span class="n">kd</span><span class="p">;</span>    <span class="c1">// na</span>
<span class="p">};</span>

<span class="n">RobotCommand</span> <span class="nf">makeCommand</span><span class="p">(</span><span class="k">const</span> <span class="n">RobotState</span><span class="o">&amp;</span> <span class="n">state</span><span class="p">,</span>
                         <span class="k">const</span> <span class="n">IDSolution</span><span class="o">&amp;</span> <span class="n">sol</span><span class="p">,</span>
                         <span class="kt">double</span> <span class="n">dt</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">Vector</span> <span class="n">v_next</span> <span class="o">=</span> <span class="n">state</span><span class="p">.</span><span class="n">v</span> <span class="o">+</span> <span class="n">sol</span><span class="p">.</span><span class="n">qddot_sol</span> <span class="o">*</span> <span class="n">dt</span><span class="p">;</span>
  <span class="n">Vector</span> <span class="n">q_next</span> <span class="o">=</span> <span class="n">pinocchio</span><span class="o">::</span><span class="n">integrate</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">state</span><span class="p">.</span><span class="n">q</span><span class="p">,</span> <span class="n">v_next</span> <span class="o">*</span> <span class="n">dt</span><span class="p">);</span>

  <span class="n">RobotCommand</span> <span class="n">cmd</span><span class="p">;</span>
  <span class="n">cmd</span><span class="p">.</span><span class="n">q</span> <span class="o">=</span> <span class="n">actuatedPosition</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">q_next</span><span class="p">);</span>
  <span class="n">cmd</span><span class="p">.</span><span class="n">qdot</span> <span class="o">=</span> <span class="n">actuatedVelocity</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">v_next</span><span class="p">);</span>
  <span class="n">cmd</span><span class="p">.</span><span class="n">tau</span> <span class="o">=</span> <span class="n">sol</span><span class="p">.</span><span class="n">tau_sol</span><span class="p">;</span>
  <span class="k">return</span> <span class="n">cmd</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>The important point is not the exact helper function names. The important point is that the matrix products expose the vector spaces, and the code should preserve those spaces instead of treating every quantity as just another <code class="language-plaintext highlighter-rouge">Vector</code>.</p> <hr/> <h2 id="9-summary">9. Summary</h2> <p>Floating-base robot representation is the first hard problem in WBC software.</p> <p>For fixed-base robots, it is often acceptable to think:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">q</span>    <span class="o">=</span> <span class="n">joint</span> <span class="n">position</span>
<span class="n">qdot</span> <span class="o">=</span> <span class="n">joint</span> <span class="n">velocity</span>
<span class="n">tau</span>  <span class="o">=</span> <span class="n">joint</span> <span class="n">torque</span>
</code></pre></div></div> <p>For floating-base robots, this is not enough.</p> <p>The configuration contains base pose and actuated joint positions:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">q</span> <span class="o">=</span> <span class="p">[</span><span class="n">base_pose</span><span class="p">,</span> <span class="n">joint_positions</span><span class="p">]</span>
</code></pre></div></div> <p>The generalized velocity contains base twist and actuated joint velocities:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">v</span> <span class="o">=</span> <span class="p">[</span><span class="n">base_twist</span><span class="p">,</span> <span class="n">joint_velocities</span><span class="p">]</span>
</code></pre></div></div> <p>The actuator torque contains only actuated joint torques:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">tau</span> <span class="o">=</span> <span class="p">[</span><span class="n">joint_torques</span><span class="p">]</span>
</code></pre></div></div> <p>Therefore:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">q</span><span class="p">.</span><span class="n">size</span><span class="p">()</span>     <span class="o">==</span> <span class="n">nq</span>
<span class="n">v</span><span class="p">.</span><span class="n">size</span><span class="p">()</span>     <span class="o">==</span> <span class="n">nv</span>
<span class="n">qddot</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">==</span> <span class="n">nv</span>
<span class="n">tau</span><span class="p">.</span><span class="n">size</span><span class="p">()</span>   <span class="o">==</span> <span class="n">na</span>
</code></pre></div></div> <p>For a floating-base robot with quaternion base orientation:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">nq</span> <span class="o">=</span> <span class="mi">7</span> <span class="o">+</span> <span class="n">na</span>
<span class="n">nv</span> <span class="o">=</span> <span class="mi">6</span> <span class="o">+</span> <span class="n">na</span>
<span class="n">tau</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">=</span> <span class="n">na</span>
</code></pre></div></div> <p>This is why floating-base robots are underactuated in the WBC formulation.</p> <p>The mechanism may have actuators at nearly every joint, but the full dynamical system also includes the floating base. That base is not directly actuated. It is controlled indirectly through joint torques, kinematic chains, support contacts, and whole-body dynamics.</p> <p>A good <code class="language-plaintext highlighter-rouge">RobotSystem</code> API should prevent configuration space, tangent space, actuator space, and contact force space from being casually mixed.</p> <p>That separation is not just code style.</p> <p>It is the foundation that makes the rest of WBC possible.</p> <p>In the next post, we move one layer up and look at how a pose target becomes a task-space acceleration constraint.</p>]]></content><author><name></name></author><category term="Whole Body Control"/><category term="Floating Base"/><category term="Software"/><summary type="html"><![CDATA[How floating-base WBC code represents q, v, actuator torque, generalized vector packing, Eigen matrix products, and solver-to-command boundaries.]]></summary></entry><entry xml:lang="en"><title type="html">[WBC 01] - Whole Body Control as Software Architecture</title><link href="https://dongho.dev/wbc01/" rel="alternate" type="text/html" title="[WBC 01] - Whole Body Control as Software Architecture"/><published>2026-06-13T00:00:00+00:00</published><updated>2026-06-13T00:00:00+00:00</updated><id>https://dongho.dev/wbc01</id><content type="html" xml:base="https://dongho.dev/wbc01/"><![CDATA[<h1 id="wbc01-what-is-whole-body-control">WBC01. What Is Whole-Body Control?</h1> <p>Whole-Body Control, or WBC, is a term that appears frequently in humanoid robotics.</p> <p>The phrase is used in several different contexts. In classical robotics, WBC often refers to a model-based controller that coordinates multiple tasks under contact and actuation constraints. More recently, the same phrase also appears in learning-based humanoid control, where a neural policy coordinates locomotion, manipulation, balance, and recovery behaviors across the full body.</p> <p>These usages are related, but they are not identical.</p> <p>At the most general level, WBC means that the robot is not controlled as a set of isolated limbs. The arm, legs, torso, contacts, balance, and posture are controlled together as one coupled system.</p> <p>For a humanoid, this matters immediately.</p> <p>If the robot reaches toward an object, the hand trajectory is only one part of the problem. The robot also has to maintain balance, avoid slipping, respect joint limits, keep the torso stable, and stay within actuator limits. A controller that only tracks the hand pose while ignoring the rest of the body is not a whole-body controller. It is an arm controller attached to a robot that may fall over.</p> <p>That is the basic motivation.</p> <p>WBC is the control layer that tries to answer the following question:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Given a robot with many degrees of freedom,
a floating base,
contacts with the environment,
and multiple objectives,

what should the whole robot do right now?
</code></pre></div></div> <p>The exact answer depends on the control paradigm. In classical model-based WBC, this question is usually answered through rigid-body dynamics, task Jacobians, contact constraints, and optimization. In learning-based WBC, this question may be answered by a neural policy trained across many simulated situations.</p> <p>This post starts from the model-based definition, then discusses how it relates to modern learning-based WBC.</p> <hr/> <h2 id="1-why-wbc-is-not-just-task-space-control">1. Why WBC is not just task-space control</h2> <p>A fixed-base manipulator is a useful starting point.</p> <p>For a conventional 6-DoF or 7-DoF robot arm bolted to a table, the main objective is often end-effector pose tracking.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>desired end-effector position
desired end-effector orientation
</code></pre></div></div> <p>The control structure is relatively direct:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>desired end-effector motion
→ Jacobian
→ joint velocity, acceleration, or torque
</code></pre></div></div> <p>The base is fixed to the world. The robot motion is mostly described by the actuated joints.</p> <p>In that setting, it is often acceptable to think:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>q   ≈ joint positions
q̇   ≈ joint velocities
τ   ≈ joint torques
</code></pre></div></div> <p>This approximation is no longer sufficient for humanoids.</p> <p>A humanoid has a floating base. The torso is not attached to the world. The robot is connected to the environment only through contacts: feet on the ground, hands on objects, or other body parts depending on the task.</p> <p>The floating base has six degrees of freedom:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>3 translational DoF
3 rotational DoF
</code></pre></div></div> <p>But there is no actuator directly attached to the floating base in world coordinates. The motors are located at the joints. As a result, the robot controls its base indirectly through joint torques and contact forces.</p> <p>The control path looks like this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>joint torques
→ joint motion
→ contact forces
→ whole-body dynamics
→ base and CoM motion
</code></pre></div></div> <p>This is the main difference from a fixed-base arm.</p> <p>A humanoid does not simply move its hand. It moves its hand while maintaining a physically feasible whole-body state.</p> <p>For example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>The hand should move toward the object.
The torso should remain balanced.
The center of mass should stay in a stable region.
The feet should not slip.
The joints should avoid hard limits.
The actuator torques should remain within limits.
</code></pre></div></div> <p>These objectives do not have equal priority.</p> <p>Balance is usually more important than hand tracking. Contact feasibility is more important than posture preference. Torque limits are not optional. Friction constraints are not tuning suggestions.</p> <p>This naturally leads to task hierarchy.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Higher-priority objectives:
  maintain balance
  preserve contacts
  satisfy physical constraints

Lower-priority objectives:
  track hand pose
  regulate posture
  use redundancy efficiently
</code></pre></div></div> <p>This is the classical motivation for WBC.</p> <p>WBC is not just “control for robots with many joints.” It is a framework for coordinating many objectives on a floating-base, underactuated, contact-constrained robot.</p> <hr/> <h2 id="2-classical-model-based-wbc">2. Classical model-based WBC</h2> <p>A useful reference point is the Stanford line of whole-body control work by Sentis and Khatib.</p> <p>In the 2005 IJHR paper, <em>Synthesis of Whole-Body Behaviors Through Hierarchical Control of Behavioral Primitives</em>, whole-body behavior is described as the composition of multiple behavioral primitives. These primitives can represent different movement criteria: center of gravity behavior, hand behavior, leg behavior, head behavior, body posture, joint-limit constraints, contacts, collision avoidance, and so on.</p> <p>The important part is the hierarchy.</p> <p>The paper organizes primitives into three broad categories:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. Constraints
2. Operational tasks
3. Postures
</code></pre></div></div> <p>Constraints are the highest priority. They represent conditions that should not be violated, such as contacts, joint limits, near-body obstacles, and self-collision constraints.</p> <p>Operational tasks are the main tasks the robot wants to accomplish, such as manipulation, locomotion, vision, hand tracking, foot tracking, or center-of-mass control.</p> <p>Postures use the residual redundancy. They can regulate body shape, symmetry, comfort, effort, or other secondary objectives.</p> <p>This hierarchy is a central idea.</p> <p>The robot should not allow an operational task to violate a constraint. The hand task should not break a contact constraint. A posture objective should not disturb balance. Lower-priority behaviors are projected into the null space of higher-priority behaviors.</p> <p>In simple terms:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>constraints first
tasks second
posture last
</code></pre></div></div> <p>The 2006 ICRA paper, <em>A Whole-Body Control Framework for Humanoids Operating in Human Environments</em>, extends this view into a more explicit humanoid control framework. It discusses control primitives, task prioritization, operational-space control, free-floating dynamics, and support contacts.</p> <p>This is where WBC becomes more clearly connected to humanoids.</p> <p>A humanoid is modeled as a free-floating system with six virtual unactuated degrees of freedom attached to the base. Supporting contacts, such as feet on the ground, are included in the control hierarchy. The controller must account for both free-floating dynamics and reaction forces at the contact points.</p> <p>This is the model-based WBC view:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>The robot has a dynamics model.
The base is unactuated.
Contacts create support forces.
Tasks are represented through Jacobians.
Constraints have priority.
Posture uses remaining redundancy.
The controller computes feasible whole-body motion and force.
</code></pre></div></div> <p>This is different from a simple end-effector controller.</p> <p>The controller is not only asking:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Where should the hand go?
</code></pre></div></div> <p>It is asking:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Can the hand move there
while contacts remain feasible,
balance is maintained,
torques stay within limits,
and higher-priority constraints are not violated?
</code></pre></div></div> <hr/> <h2 id="3-a-compact-equation-a-large-implementation">3. A compact equation, a large implementation</h2> <p>A common starting point for floating-base control is the rigid-body dynamics equation:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>M(q) q̈ + h(q, q̇) = Sᵀτ + Jᵀλ
</code></pre></div></div> <p>The notation varies by paper and codebase, but the meaning is roughly:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>M(q)       mass matrix
q̈         generalized acceleration
h(q, q̇)   nonlinear effects: Coriolis, centrifugal, gravity
Sᵀτ        actuator torques mapped into generalized coordinates
Jᵀλ        contact forces mapped into generalized coordinates
</code></pre></div></div> <p>The equation is compact. The implementation is not.</p> <p>Each symbol implies a software decision.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>How is q represented?
Does q include the floating base?
Is the base orientation stored as a quaternion?
Is q̇ the same dimension as q?
Which coordinates are actuated?
How is the selection matrix S defined?
How are contact Jacobians stacked?
What is the ordering of λ?
Who owns the robot model?
Who updates the dynamics?
Who decides which contacts are active?
</code></pre></div></div> <p>This is why WBC quickly becomes a software architecture problem.</p> <p>The equation itself is not enough. A deployable controller needs a structure for robot state, dynamics computation, task construction, contact management, optimization, state machines, command conversion, and hardware communication.</p> <hr/> <h2 id="4-learning-based-wbc">4. Learning-based WBC</h2> <p>Recently, the phrase WBC has also become common in learning-based humanoid control.</p> <p>In this context, WBC often means a learned policy that coordinates the entire body rather than controlling locomotion and manipulation as separate modules.</p> <p>For example, learning-based whole-body control methods may train a policy to handle:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>locomotion
manipulation
disturbance recovery
whole-body motion imitation
multi-mode command following
loco-manipulation
</code></pre></div></div> <p>This direction is natural.</p> <p>Some behaviors are difficult to specify cleanly with a hand-written model-based controller. Footstep recovery, dynamic balance recovery, rich contact transitions, and multi-contact behaviors can be hard to encode as explicit tasks and constraints. Reinforcement learning can expose the robot to many situations in simulation and optimize a policy over a broad distribution of states.</p> <p>Learning-based WBC also changes the software shape.</p> <p>At runtime, the control interface may look simple:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>observation → neural network → action
</code></pre></div></div> <p>The action might be joint position targets, velocity targets, torque commands, residual commands, contact schedules, or task-space targets. The architecture can appear much simpler than a classical WBC stack with task objects, contact specifications, QP matrices, and command adapters.</p> <p>But the complexity has not disappeared. It has moved.</p> <p>In learning-based WBC, much of the complexity moves into:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>reward design
curriculum design
policy architecture
simulation fidelity
domain randomization
dataset quality
action space design
debugging policy failures
sim-to-real transfer
</code></pre></div></div> <p>The controller may be simple to run, but it can be difficult to inspect.</p> <p>If a model-based controller fails, we can often ask specific questions:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Which task became infeasible?
Which constraint is active?
Which torque limit is binding?
Which Jacobian is singular?
Which contact force violated the friction cone?
</code></pre></div></div> <p>With a learned policy, the failure mode may be harder to localize. The policy may output an action that is internally consistent from the network’s perspective but physically difficult for the low-level controller or hardware to track.</p> <p>This is one of the main practical issues.</p> <p>A learned policy can generate a target that is outside the tracking capability of the lower-level system. It may request a motion that violates actuator bandwidth, produces excessive torque demand, changes contact too aggressively, or assumes a contact condition that is not actually present.</p> <p>In that sense, learning-based WBC is not automatically simpler. It is simpler at the runtime interface, but often harder at the training, debugging, and validation layers.</p> <hr/> <h2 id="5-model-based-and-learning-based-wbc-have-different-purposes">5. Model-based and learning-based WBC have different purposes</h2> <p>The point is not that model-based WBC is still important and therefore learning-based control is wrong.</p> <p>That framing is not useful.</p> <p>The better view is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>model-based WBC and learning-based WBC solve different parts of the problem
</code></pre></div></div> <p>Learning-based control is strong when the behavior is difficult to explicitly model or hand-design.</p> <p>Examples include:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>disturbance recovery
dynamic footstep adjustment
multi-contact adaptation
whole-body motion imitation
locomotion-manipulation coordination
robust behavior over a wide state distribution
</code></pre></div></div> <p>Instead of writing every contact transition and recovery strategy by hand, the policy can learn behavior from simulation, demonstrations, rewards, or motion data.</p> <p>Model-based WBC is strong when we want interpretability and direct control over physical behavior.</p> <p>Examples include:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>task-space position tracking
force control
impedance control
torque limit handling
contact force constraints
task prioritization
posture regulation
debuggable constraint violations
</code></pre></div></div> <p>In operational-space-control-style WBC, position and force control have a clear physical interpretation. Gains correspond to stiffness, damping, or desired impedance. A task has a Jacobian. A force has a frame. A constraint has a direction. A torque limit has a meaning.</p> <p>This makes model-based WBC useful when we care about predictable low-level behavior.</p> <p>For example, if we want to change the apparent stiffness of a hand task, a model-based controller gives us a direct knob:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>increase Kp → higher apparent stiffness
increase Kd → more damping
change force target → different contact behavior
</code></pre></div></div> <p>That does not mean tuning is easy. It means the tuning parameter has a physical interpretation.</p> <p>A neural policy can also learn compliant behavior, but the relationship between a network weight and the resulting impedance is not usually something we can inspect directly.</p> <p>So the difference is not:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>model-based = old
learning-based = new
</code></pre></div></div> <p>The difference is closer to:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>learning-based control:
  learns behavior over many situations

model-based WBC:
  exposes physical structure and enforces interpretable objectives
</code></pre></div></div> <p>Both are useful.</p> <p>For real humanoid deployment, a hybrid architecture is likely.</p> <p>A learned policy may generate high-level intent, task targets, contact schedules, recovery behaviors, or residual commands. A model-based layer may still handle tracking, impedance, torque limits, contact constraints, safety filters, or hardware command conversion.</p> <p>The boundary will depend on the robot and the task.</p> <hr/> <h2 id="6-why-model-based-wbc-software-is-difficult">6. Why model-based WBC software is difficult</h2> <p>Learning-based WBC can have a simple runtime interface:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>input observation
→ neural network
→ output action
</code></pre></div></div> <p>Model-based WBC does not look like that.</p> <p>The controller must explicitly represent the robot, tasks, constraints, contacts, priorities, dynamics, solver variables, and actuator commands.</p> <p>This becomes clear when looking at an inverse-dynamics WBC formulation.</p> <p>A typical IHWBC-style problem may optimize over generalized acceleration and reaction force:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>minimize over q̈, f_r

  Σᵢ wᵢ || Jᵢ q̈ + J̇ᵢ q̇ - ẍᵢᵈ ||²
  + w_f || f_rᵈ - f_r ||²
  + λ_q || q̈ ||²
  + λ_f || f_r ||²
</code></pre></div></div> <p>subject to constraints such as:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>floating-base dynamics
friction cone constraints
reaction force limits
joint acceleration limits
torque limits
</code></pre></div></div> <p>The important point is not the exact equation.</p> <p>The important point is what the equation requires from the software.</p> <p>The WBC input is not simply:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x_des
λ_des
</code></pre></div></div> <p>The input is closer to:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>robot state
active tasks
active contacts
task priorities
task gains
desired accelerations
force targets
contact constraints
actuator limits
</code></pre></div></div> <p>A task is not just a target pose.</p> <p>A task contains information such as:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>controlled frame
task dimension
task Jacobian
J̇q̇ term
desired position or force
desired velocity
desired acceleration
feedback gains
weight or hierarchy level
activation condition
</code></pre></div></div> <p>A contact specification contains information such as:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>contact frame
contact Jacobian
contact force dimension
friction coefficient
normal direction
force limits
contact activation state
</code></pre></div></div> <p>A solver solution is also not automatically a hardware command.</p> <p>The solver may output:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>q̈_sol
f_r_sol
τ_sol
</code></pre></div></div> <p>But the actuator interface may require:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>q_cmd
qdot_cmd
kp_cmd
kd_cmd
tau_ff_cmd
</code></pre></div></div> <p>Therefore, the controller needs a command conversion layer:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>q̈_sol
→ integrate
→ qdot_cmd
→ q_cmd
→ clamp
→ rate limit
→ hardware command
</code></pre></div></div> <p>This is where WBC software becomes complicated.</p> <p>The difficulty is not only solving the QP. The difficulty is keeping the software boundaries clean.</p> <hr/> <h2 id="7-wbc-as-software-architecture">7. WBC as software architecture</h2> <p>For this project, I want to treat WBC as a software architecture problem.</p> <p>The goal is not to implement one isolated model-based controller. The goal is to design a ROS 2 control-based architecture that can support several control modes under a common robot interface.</p> <p>The architecture should be able to support:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>model-based WBC
learning-based planning
learning-based WBC
model-based trajectory optimization
MPC
low-level hardware command conversion
</code></pre></div></div> <p>The central idea is to separate responsibilities.</p> <p>A practical stack should have at least the following layers:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>RobotSystem
  Owns robot state, kinematics, dynamics, frame transforms, and model queries.

Task
  Represents motion, force, constraint, or posture objectives.

ContactSpec
  Represents active contacts, contact Jacobians, friction cones, and force limits.

Planner / Policy
  Generates task targets, contact schedules, trajectories, or policy actions.

WBCSolver
  Converts active tasks and contacts into a dynamics-level solution.

CommandAdapter
  Converts solver or policy outputs into hardware-compatible commands.

FSM
  Selects the active control mode, task set, contact set, and safety behavior.

HardwareInterface
  Sends commands to drivers and reads actuator states.
</code></pre></div></div> <p>The important part is that these layers should not collapse into each other.</p> <p>The solver should not know about CAN/EtherCAT/RS485 packet encoding. The hardware interface should not know task hierarchy. A task should not directly write motor commands. A learned policy should not bypass safety and command limits unless that is an explicit design choice. The FSM should coordinate modes, not secretly patch every controller failure.</p> <p>This separation is what makes hybrid control possible.</p> <p>For example, a learned policy and a model-based WBC solver can both produce commands through the same command adapter, or they can both generate targets for a shared low-level controller. A model-based trajectory optimizer can generate references that become tasks. MPC can provide short-horizon targets. A learned policy can provide residuals or contact schedules.</p> <p>The architecture should make these combinations possible without rewriting the hardware layer every time.</p> <p>That is the main project goal. `</p> <hr/> <h2 id="references">References</h2> <ul> <li>O. Khatib, “A Unified Approach for Motion and Force Control of Robot Manipulators: The Operational Space Formulation,” 1987.</li> <li>L. Sentis and O. Khatib, “Synthesis of Whole-Body Behaviors Through Hierarchical Control of Behavioral Primitives,” <em>International Journal of Humanoid Robotics</em>, 2005.</li> <li>L. Sentis and O. Khatib, “A Whole-Body Control Framework for Humanoids Operating in Human Environments,” <em>IEEE International Conference on Robotics and Automation</em>, 2006.</li> <li>Z. Fu, X. Cheng, and D. Pathak, “Deep Whole-Body Control: Learning a Unified Policy for Manipulation and Locomotion,” CoRL, 2022.</li> <li>T. He et al., “HOVER: Versatile Neural Whole-Body Controller for Humanoid Robots,” 2024.</li> <li>Agility Robotics, “Training a Whole-Body Control Foundation Model,” 2025.</li> <li>NVIDIA, “GR00T-WholeBodyControl.”</li> </ul>]]></content><author><name></name></author><category term="Whole Body Control"/><category term="Software"/><summary type="html"><![CDATA[A technical overview of Whole Body Control and the software boundaries needed to implement it on floating-base robots.]]></summary></entry><entry xml:lang="en"><title type="html">[Robot Hardware 04] - Actuators (3): QDD Actuators</title><link href="https://dongho.dev/hardware04/" rel="alternate" type="text/html" title="[Robot Hardware 04] - Actuators (3): QDD Actuators"/><published>2025-12-06T00:00:00+00:00</published><updated>2025-12-06T00:00:00+00:00</updated><id>https://dongho.dev/hardware-04</id><content type="html" xml:base="https://dongho.dev/hardware04/"><![CDATA[<p><a href="/projects/hardware-kr/">Robot Hardware from a Physical AI perspective</a></p> <p>The high-ratio systems discussed in the previous notes, such as Harmonic Drives, use a small motor and a high-ratio reducer to obtain torque. A <strong>QDD (Quasi-Direct Drive)</strong> actuator takes almost the opposite design philosophy.</p> <p>The main goals of QDD are:</p> <ol> <li><strong>Low Mechanical Impedance:</strong> Keep the mechanical effect of motor rotor inertia low.</li> <li><strong>High Transparency:</strong> Minimize mechanical nonlinearities such as friction and backlash.</li> <li><strong>High Bandwidth:</strong> Respond quickly enough to follow high-frequency force and motion commands.</li> </ol> <p>These properties are useful in systems that interact dynamically with the environment, such as quadrupeds and humanoids.</p> <h2 id="structure-and-design">Structure and Design</h2> <p>QDD is not usually a fully direct-drive system. Instead, it uses a low reduction ratio, commonly around <strong>6:1 to 10:1</strong>.</p> <p>Because the reducer no longer provides a large torque multiplier, QDD actuators rely on <strong>high-torque-density motors</strong>.</p> <ul> <li><strong>Motor:</strong> Large-diameter, high-pole-count <strong>BLDC outrunner</strong> motors are common because a larger air-gap radius helps produce more torque.</li> <li><strong>Reducer:</strong> Planetary gears or cycloidal drives are often used. To keep the package compact, the reducer may be placed inside the open space of the motor stator.</li> </ul> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hardware/pancake-480.webp 480w,/assets/img/hardware/pancake-800.webp 800w,/assets/img/hardware/pancake-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hardware/pancake.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Example QDD actuator [1] </div> </div> </div> <figure> <iframe width="720" height="405" src="https://www.youtube.com/embed/Mhxz2Bj2RXA" title="MIT Mini Cheetah Actuator Teardown" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen=""> </iframe> <figcaption style="text-align: center;"> <em>MIT Mini Cheetah Actuator Teardown</em> </figcaption> </figure> <p>This structure makes it possible to build a compact joint module where the motor and reducer are tightly integrated.</p> <h2 id="core-concept-force-motion-transparency">Core Concept: Force-Motion Transparency</h2> <p>The central performance concept for QDD is <strong>force-motion transparency</strong>.</p> <p>This term originally comes from teleoperation. It describes how accurately force and motion at the input side are transmitted to the output side, and how cleanly reaction forces at the output side are transmitted back to the input side.</p> <p>Using a mechanical impedance model $Z(s)$, high transparency means that internal inertia $M$ and friction $B$ should be as small as possible.</p> \[Z(s) = \frac{F(s)}{v(s)} = Ms + B + \frac{K}{s}\] <p>By lowering the reduction ratio, QDD reduces both high-frequency distortion from <strong>inertia</strong> and low-frequency distortion from <strong>friction</strong>. The goal is to preserve transparency across a wide frequency range.</p> <h2 id="two-ways-to-define-qdd">Two Ways to Define QDD</h2> <p>There is no single strict numerical definition of QDD. In practice, it is usually discussed from two perspectives.</p> <h3 id="1-dynamic-transparency">1. Dynamic Transparency</h3> <p>This perspective focuses on impact mitigation and high-speed control. The key quantity is <strong>reflected inertia</strong>.</p> <p>Motor rotor inertia $J_m$ is amplified through a reducer by the square of the gear ratio $N^2$:</p> \[J_{ref} = J_m \times N^2\] <p>In a high-ratio system, such as 100:1, rotor inertia is amplified by 10,000 times. The joint becomes dynamically sluggish. In a QDD system, such as 6:1, the amplification is only 36 times.</p> <p><strong>Benefits of low reflected inertia:</strong></p> <ul> <li><strong>Impact mitigation:</strong> The actuator can mechanically absorb or pass through impact energy during ground contact or collision.</li> <li><strong>High control bandwidth:</strong> Low inertia allows faster acceleration and better tracking of high-frequency commands.</li> </ul> <h3 id="2-quasi-static-force-transparency">2. Quasi-Static Force Transparency</h3> <p>This perspective focuses on how accurately force can be estimated from motor current.</p> <p>Even if the gear ratio is moderately high, around 20:1 for example, an actuator may still be considered QDD-like if mechanical nonlinearities such as friction and hysteresis are sufficiently low.</p> <ul> <li><strong>High backdrivability:</strong> The output shaft should move smoothly under external force, even when power is off.</li> <li><strong>Linearity:</strong> Low friction helps preserve a linear relationship between current $I$ and torque $\tau$.</li> </ul> <h2 id="proprioceptive-actuation">Proprioceptive Actuation</h2> <p>One control idea enabled by the force transparency of QDD hardware is <strong>proprioceptive actuation</strong>.</p> <p>This means estimating and controlling external force using only motor <strong>current</strong>, without a separate torque sensor.</p> <p>In a QDD system:</p> <ol> <li>Gear friction and mechanical nonlinearities are low.</li> <li>Reflected inertia is small, so dynamic disturbance is reduced.</li> <li>The relationship <strong>motor current $\propto$ external torque</strong> becomes usable.</li> </ol> <p>This allows force sensing and force control with a simpler sensor configuration.</p> <blockquote> <p><strong>Note:</strong> This idea was introduced in the MIT Biomimetic Robotics Lab paper <em>“Proprioceptive Actuator Design in the MIT Cheetah” [2]</em>. I will discuss it in more detail later.</p> </blockquote> <h2 id="synergy-with-learning-based-control-and-its-limits">Synergy with Learning-Based Control and Its Limits</h2> <h3 id="fit-with-learning-based-control">Fit with Learning-Based Control</h3> <p>QDD offers hardware properties that are useful when deploying learning-based controllers, especially policies trained with <strong>reinforcement learning</strong>, on real robots.</p> <p>This is not because QDD changes the learning algorithm itself. Rather, it helps the real actuator behave more like the actuator assumed by the policy.</p> <ul> <li> <p><strong>Reduced sim-to-real gap:</strong> High-ratio transmissions often introduce large friction, backlash, stiction, and complex transmission nonlinearities. QDD reduces many of these effects, so the gap between simulated actuator dynamics and real hardware response becomes smaller. This can reduce performance loss when transferring policies from simulation to hardware.</p> </li> <li> <p><strong>Backdrivability:</strong> Because QDD has high mechanical transparency, the actuator does not strongly reject external contact or unexpected collision. This is useful in manipulation and locomotion, where frequent contact makes overly rigid behavior unstable.</p> </li> </ul> <h3 id="limitation-heat">Limitation: Heat</h3> <p>The main weakness of QDD is <strong>heat</strong>.</p> <p>Because the reduction ratio is low, the motor itself must generate much of the required torque. That means high current. Copper loss in the motor, $P_{loss} = I^2R$, grows with the square of current, so heat rises quickly.</p> <ul> <li><strong>Low torque multiplication:</strong> With less help from the gear ratio, the actuator depends heavily on the motor’s own torque constant.</li> <li><strong>Thermal management:</strong> Static tasks that require the robot to hold high gravitational loads can be inefficient and may overheat the motor.</li> </ul> <p>For this reason, QDD actuators are often better suited to <strong>dynamic tasks</strong> where the robot is continuously moving, exchanging energy, and using regenerative effects. In real systems, motor thermal modeling and cooling design are essential.</p> <h3 id="references">References</h3> <p>[1] https://www.semanticscholar.org/paper/A-low-cost-modular-actuator-for-dynamic-robots-Katz/80732f8a46655aa4a1037a7fbdc154f4ceb33c50</p> <p>[2] https://fab.cba.mit.edu/classes/865.18/motion/papers/mit-cheetah-actuator.pdf</p>]]></content><author><name></name></author><category term="Hardware Development"/><summary type="html"><![CDATA[Robot hardware from a Physical AI perspective - QDD actuators]]></summary></entry><entry xml:lang="ko"><title type="html">[로봇 하드웨어 04] - 액추에이터(3) : QDD 액추에이터</title><link href="https://dongho.dev/hardware04-kr/" rel="alternate" type="text/html" title="[로봇 하드웨어 04] - 액추에이터(3) : QDD 액추에이터"/><published>2025-12-06T00:00:00+00:00</published><updated>2025-12-06T00:00:00+00:00</updated><id>https://dongho.dev/hardware-04-kr</id><content type="html" xml:base="https://dongho.dev/hardware04-kr/"><![CDATA[<p><a href="/projects/hardware-kr/">Physical AI 관점에서 본 다관절 로봇 하드웨어 시리즈</a></p> <p>이전 포스트에서 다룬 고감속비 기반 시스템(Harmonic Drive 등)이 소형 모터와 고감속기를 조합하여 토크를 확보하는 방식이라면, <strong>QDD (Quasi-Direct Drive)</strong> 액추에이터는 상반된 설계 철학을 가집니다.</p> <p>QDD의 주요 설계 목표는 다음과 같습니다.</p> <ol> <li><strong>Low Mechanical Impedance:</strong> 모터 로터의 관성을 기계적으로 낮게 유지할 것.</li> <li><strong>High Transparency:</strong> 마찰, 백래시 등 기계적 비선형성을 최소화할 것.</li> <li><strong>High Bandwidth:</strong> 높은 응답성을 통해 고주파수의 힘과 운동 명령을 수행할 것.</li> </ol> <p>이러한 특성은 4족 보행 로봇이나 휴머노이드와 같이 외부 환경과의 동적 상호작용(Dynamic Interaction)이 잦은 시스템에 자주 쓰입니다.</p> <h2 id="구조-및-설계">구조 및 설계</h2> <p>QDD는 완전한 무감속(Direct Drive) 방식은 아니며, 통상 <strong>6:1 ~ 10:1</strong> 수준의 낮은 기어비를 사용합니다.</p> <p>낮은 기어비로 인한 토크 부족을 보완하기 위해 <strong>High Torque Density 모터</strong>를 사용합니다.</p> <ul> <li><strong>Motor:</strong> 공극 반경(Air gap radius)을 키워 토크를 확보하기 위해 직경이 크고 극수(Pole count)가 많은 <strong>BLDC Outrunner</strong> 모터를 주로 사용합니다.</li> <li><strong>Reducer:</strong> 주로 <strong>Planetary Gear</strong>나 <strong>Cycloidal Drive</strong>를 사용하며, 전체 부피를 줄이기 위해 감속기를 모터 스테이터 내부 공간에 배치하는 설계를 취합니다.</li> </ul> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hardware/pancake-480.webp 480w,/assets/img/hardware/pancake-800.webp 800w,/assets/img/hardware/pancake-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hardware/pancake.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> QDD 액추에이터 예시 [1] </div> </div> </div> <figure> <iframe width="720" height="405" src="https://www.youtube.com/embed/Mhxz2Bj2RXA" title="MIT Mini Cheetah Actuator Teardown" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen=""> </iframe> <figcaption style="text-align: center;"> <em>MIT Mini Cheetah Actuator Teardown</em> </figcaption> </figure> <p>이러한 구조를 통해 모터와 감속기가 일체화된 컴팩트한 관절 모듈 구성이 가능합니다.</p> <h2 id="핵심-개념-힘-운동-투명성-force-motion-transparency">핵심 개념: 힘-운동 투명성 (Force-Motion Transparency)</h2> <p>QDD의 핵심 성능 지표는 <strong>Force-Motion Transparency</strong>입니다. 이는 본래 <strong>Teleoperation</strong> 분야의 개념으로, 입력단의 힘과 운동이 왜곡 없이 출력단으로 전달되고, 반대로 출력단의 반력(Reaction force)이 입력단으로 손실 없이 전달되는 정도를 의미합니다.</p> <p>기계적 임피던스 $Z(s)$ 모델에서 이를 해석하면, 시스템 내부의 관성($M$)과 마찰($B$)이 최소화되어야 함을 뜻합니다.</p> \[Z(s) = \frac{F(s)}{v(s)} = Ms + B + \frac{K}{s}\] <p>QDD는 감속비를 낮춤으로써 고주파 영역의 왜곡 요인인 <strong>관성($M$)</strong>과 저주파 영역의 왜곡 요인인 <strong>마찰($B$)</strong>을 동시에 저감하여, 전 주파수 대역에서 높은 <strong>Transparency</strong>를 확보하는 것을 목표로 합니다.</p> <h2 id="qdd를-정의하는-두-가지-관점">QDD를 정의하는 두 가지 관점</h2> <p>QDD를 구분하는 엄밀한 정량적 기준은 없으나, 학계와 업계에서는 주로 다음 두 가지 관점에서 정의합니다.</p> <h3 id="1-동적-투명성-관점-dynamic-transparency">1. 동적 투명성 관점 (Dynamic Transparency)</h3> <p>충격 완화와 고속 제어 성능에 초점을 맞춘 관점입니다. 핵심 인자는 <strong>Reflected Inertia(반사 관성)</strong>입니다.</p> <p>모터 로터의 관성($J_m$)은 감속기를 거치며 기어비의 제곱($N^2$)에 비례하여 증폭됩니다.</p> \[J_{ref} = J_m \times N^2\] <p>고감속비 시스템(예: 100:1)에서는 로터 관성이 10,000배로 증폭되어 관절이 동적으로 둔감해지는 반면, QDD(예: 6:1)는 관성 증폭이 36배 수준에 그칩니다.</p> <p><strong>낮은 Reflected Inertia의 이점:</strong></p> <ul> <li><strong>Impact Mitigation:</strong> 지면 접촉이나 충돌 시 발생하는 충격 에너지를 기계적으로 흡수하거나 흘려보낼 수 있습니다.</li> <li><strong>High Control Bandwidth:</strong> 시스템의 관성이 작아 빠른 가감속과 고주파수 제어 명령 추종이 가능합니다.</li> </ul> <h3 id="2-준정적-힘-투명성-관점-quasi-static-force-transparency">2. 준정적 힘 투명성 관점 (Quasi-static Force Transparency)</h3> <p>전류 기반의 힘 추정 정확도에 초점을 맞춘 관점입니다.</p> <p>감속비가 다소 높더라도(20:1 내외), 시스템의 마찰이나 히스테리시스와 같은 <strong>기계적 비선형성(Non-linearity)</strong>이 충분히 낮다면 QDD의 범주에 포함됩니다.</p> <ul> <li><strong>High Backdrivability:</strong> 전원이 차단된 상태에서도 외부 힘에 의해 출력축이 부드럽게 구동되어야 합니다.</li> <li><strong>Linearity:</strong> 마찰이 적어 전류($I$)와 토크($\tau$) 간의 관계가 선형성을 유지합니다.</li> </ul> <h2 id="proprioceptive-actuation">Proprioceptive Actuation</h2> <p>QDD 하드웨어의 힘 투명성 특성을 활용한 제어 방식이 <strong>Proprioceptive Actuation</strong>입니다.</p> <p>이는 별도의 <strong>Torque Sensor</strong> 없이 모터의 <strong>전류(Current)</strong> 정보만을 이용하여 외력을 추정하고 제어하는 방식을 의미합니다.</p> <p>QDD 시스템에서는:</p> <ol> <li>기어 마찰과 비선형성이 낮고,</li> <li>Reflected Inertia가 작아 동적 외란의 영향이 적으므로,</li> <li><strong>“모터 전류 $\propto$ 외부 토크”</strong>의 관계가 성립합니다.</li> </ol> <p>이를 통해 복잡한 센서 구성 없이도 <strong>Force Sensing</strong>과 <strong>Force Control</strong>을 구현할 수 있습니다.</p> <blockquote> <p><strong>참고:</strong> 이 개념은 MIT Biomimetic Robotics Lab의 논문 <em>“Proprioceptive Actuator Design in the MIT Cheetah [2]”</em>에서 소개되었으며, 자세한 내용은 나중에 다루도록 하겠습니다.</p> </blockquote> <h2 id="학습-기반-제어와의-시너지-및-한계">학습 기반 제어와의 시너지 및 한계</h2> <h3 id="학습-기반-제어rl와의-적합성">학습 기반 제어(RL)와의 적합성</h3> <p>QDD는 최근의 학습 기반 제어, 특히 <strong>강화학습(Reinforcement Learning)</strong> 기반 정책을 실제 로봇에 적용하는 데 유리한 하드웨어적 특성을 제공합니다. 이는 QDD가 학습 알고리즘 자체를 바꾸기 때문이 아니라, 정책이 전제하는 actuator behavior를 실제 시스템이 더 충실하게 재현할 수 있게 해주기 때문입니다.</p> <ul> <li> <p><strong>Sim-to-Real Gap 감소:</strong> 높은 감속비 구동계에서 흔히 문제가 되는 큰 마찰, 백래시, stiction, 그리고 복잡한 전달계 비선형성이 상대적으로 작기 때문에, 시뮬레이션에서 가정한 actuator dynamics와 실제 하드웨어의 응답 사이의 괴리가 줄어듭니다. 그 결과, 시뮬레이션에서 학습된 정책을 실제 시스템에 이식할 때 발생하는 성능 저하를 완화하는 데 도움이 됩니다.</p> </li> <li> <p><strong>Backdrivability:</strong> QDD는 외력에 대한 기계적 투명성이 높아, 환경과의 접촉이나 예상하지 못한 충돌이 발생했을 때 actuator가 이를 과도하게 거부하지 않고 자연스럽게 수용할 수 있습니다. 이는 접촉이 빈번한 조작이나 locomotion에서 정책이 지나치게 경직된 방식으로 동작하는 것을 줄이고, 실제 환경 변화에 대해 더 안정적인 상호작용을 가능하게 합니다.</p> </li> </ul> <h3 id="한계점-발열-thermal-issue">한계점: 발열 (Thermal Issue)</h3> <p>QDD의 주요 단점은 <strong>발열</strong>입니다. 낮은 기어비로 높은 토크를 생성해야 하므로 모터에 인가되는 전류량이 많습니다. 모터의 동손(Copper loss, $P_{loss} = I^2 R$)은 전류의 제곱에 비례하므로 발열량이 급격히 증가합니다.</p> <ul> <li><strong>Low Torque Constant:</strong> 기어비에 의한 토크 증폭 효과가 적어 모터 자체의 토크 상수에 의존해야 합니다.</li> <li><strong>Thermal Management:</strong> 높은 중력 부하를 지속적으로 지탱해야 하는 정적인 작업(Static holding)에서는 효율이 낮고 과열 위험이 있습니다.</li> </ul> <p>따라서 QDD 액추에이터는 정적인 고부하 작업보다는, 지속적으로 움직이며 에너지를 소비하고 회생하는 <strong>동적(Dynamic) 작업</strong>에 적합합니다. 실제 운용 시에는 모터의 열 모델링과 냉각 대책이 필수적으로 고려되어야 합니다.</p> <h3 id="references">References</h3> <p>[1] https://www.semanticscholar.org/paper/A-low-cost-modular-actuator-for-dynamic-robots-Katz/80732f8a46655aa4a1037a7fbdc154f4ceb33c50</p> <p>[2] https://fab.cba.mit.edu/classes/865.18/motion/papers/mit-cheetah-actuator.pdf</p>]]></content><author><name></name></author><category term="Hardware Development"/><summary type="html"><![CDATA[Physical AI 관점에서 본 다관절 로봇 하드웨어 - QDD 액추에이터]]></summary></entry><entry xml:lang="en"><title type="html">[Robot Hardware 03] - Cycloidal Drives</title><link href="https://dongho.dev/hardware03-03/" rel="alternate" type="text/html" title="[Robot Hardware 03] - Cycloidal Drives"/><published>2025-12-05T00:00:00+00:00</published><updated>2025-12-05T00:00:00+00:00</updated><id>https://dongho.dev/hardware-03-03</id><content type="html" xml:base="https://dongho.dev/hardware03-03/"><![CDATA[<p><a href="/hardware03/">Back to the parent post</a></p> <p>This note covers <strong>cycloidal drives</strong>, which have recently received renewed attention in legged robots and compact actuators because of their shock resistance and high torque density.</p> <p>A cycloidal drive uses an <strong>eccentric</strong> input and the resulting cycloidal motion to reduce speed and amplify torque. The gear ratio is set by the cycloid tooth geometry. In a compact package, it can deliver high torque, and more importantly, it is mechanically robust against impact.</p> <h2 id="operating-principle-and-gear-ratio">Operating Principle and Gear Ratio</h2> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hardware/cycloid_motion-480.webp 480w,/assets/img/hardware/cycloid_motion-800.webp 800w,/assets/img/hardware/cycloid_motion-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hardware/cycloid_motion.gif" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Example motion of a cycloidal reduction [1] </div> </div> </div> <p>A cycloidal drive works through three main mechanisms:</p> <ol> <li><strong>Eccentric Input:</strong> The input shaft connected to the motor carries an eccentric bearing. As the shaft rotates, it creates motion offset from the center axis.</li> <li><strong>Cycloid Motion:</strong> The cycloid disk mounted on the eccentric bearing rolls and wobbles along a cycloidal path while engaging with the ring pins of the housing.</li> <li><strong>Output Extraction:</strong> Pins connected to the output shaft pass through holes in the disk, extracting only the slow rotation of the disk as output motion.</li> </ol> <h3 id="gear-ratio-for-different-fixed-elements">Gear Ratio for Different Fixed Elements</h3> <p>The gear ratio and output direction change depending on which element is fixed. If the housing has $N$ pins and the cycloid disk has $N-1$ lobes:</p> <ul> <li> <p><strong>Outer ring fixed / disk output:</strong> When the input shaft rotates once, the disk rotates backward by one tooth pitch. Gear Ratio = $(N - 1) / 1$</p> <figure> <iframe width="720" height="405" src="https://www.youtube.com/embed/L6sBhsO_UNA" title="Cycloidal Drive - Fixed Ring" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen=""> </iframe> <figcaption style="text-align: center;"> <em>Cycloidal Drive - Fixed Ring (short)</em> </figcaption> </figure> </li> <li> <p><strong>Disk fixed / outer ring output:</strong> When the input shaft rotates once, the outer ring rotates forward by one pin pitch. Gear Ratio = $N / 1$</p> <figure> <iframe width="720" height="405" src="https://www.youtube.com/embed/7WD3s8bxU5c" title="Cycloidal Drive - Fixed Shaft" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen=""> </iframe> <figcaption style="text-align: center;"> <em>Cycloidal Drive - Fixed Shaft (short)</em> </figcaption> </figure> </li> </ul> <h2 id="2-contact-design-rolling-vs-sliding-contact">2. Contact Design: Rolling vs. Sliding Contact</h2> <p>Unlike ordinary gears that transmit power through gear-tooth meshing, a cycloidal drive is governed by kinematic constraints. Depending on the design goal, it can use either <strong>rolling contact</strong> or <strong>sliding contact</strong>.</p> <h3 id="rolling-contact">Rolling Contact</h3> <p>Bearings or cylindrical rollers are added at the housing pins or output pins to minimize friction.</p> <ul> <li><strong>Strength:</strong> Very low friction loss, high transmission efficiency, and less heat.</li> <li><strong>Limitation:</strong> It is harder to miniaturize. Higher reduction ratios require more rollers, which increases part count and weight.</li> </ul> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hardware/rolling_contact_cycloid-480.webp 480w,/assets/img/hardware/rolling_contact_cycloid-800.webp 800w,/assets/img/hardware/rolling_contact_cycloid-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hardware/rolling_contact_cycloid.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Rolling Contact Cycloid [2] </div> </div> </div> <h3 id="sliding-contact">Sliding Contact</h3> <p>Instead of adding rollers, the cycloid disk surface itself slides directly against the housing or output pins.</p> <ul> <li><strong>Strength:</strong> The structure becomes much simpler, allowing extreme miniaturization and weight reduction.</li> <li><strong>Limitation:</strong> Sliding friction is large, so efficiency drops and wear becomes a major issue.</li> </ul> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hardware/sliding_contact_cycloid-480.webp 480w,/assets/img/hardware/sliding_contact_cycloid-800.webp 800w,/assets/img/hardware/sliding_contact_cycloid-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hardware/sliding_contact_cycloid.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Sliding Contact Cycloid [3] </div> </div> </div> <p>In practical actuator design, rolling contact is often used near the central eccentric shaft, where the load is high. Sliding contact may be used between the disk and the outer housing, where the relative surface speed is lower.</p> <p>To reduce friction and wear in the sliding-contact region, the cycloid disk can also be machined from low-friction engineering plastics such as <strong>Delrin (POM)</strong> or <strong>nylon</strong> instead of metal.</p> <h2 id="3-advantages">3. Advantages</h2> <h4 id="1-high-torque-density">1. High Torque Density</h4> <p>Like a Harmonic Drive, a cycloidal drive can generate high torque in a compact volume, making joint packaging easier.</p> <h4 id="2-high-shock-resistance">2. High Shock Resistance</h4> <p>Where a spur gear may carry torque through only one or two teeth, a cycloidal drive can distribute load across roughly <strong>30 to 50%</strong> of its tooth profile at the same time.</p> <p>That load distribution makes it resistant to shock loads. It is a good fit for systems that see repeated impact, such as legged robots striking the ground or manipulators colliding with the environment.</p> <h4 id="3-high-single-stage-reduction-ratio">3. High Single-Stage Reduction Ratio</h4> <p>A single-stage cycloidal drive can reach reduction ratios from 50:1 to above 100:1, reducing the volume and complexity that would come from multi-stage reducers.</p> <h2 id="4-limitations-and-design-considerations">4. Limitations and Design Considerations</h2> <h4 id="1-manufacturing-difficulty">1. Manufacturing Difficulty</h4> <p>The part count may look small, but the required manufacturing precision is high. The cycloidal profile must be machined accurately, usually requiring precise CNC work and careful assembly.</p> <h4 id="2-tolerance-sensitivity">2. Tolerance Sensitivity</h4> <p>Tolerance errors or assembly mistakes directly degrade performance. If clearances are too tight, the drive can jam. If they are too loose, backlash increases and positioning accuracy drops.</p> <h4 id="3-profile-modification">3. Profile Modification</h4> <p>Eccentricity is a key variable that determines the curvature of the cycloid profile and the kinematic constraints of the drive.</p> <p>If the theoretical cycloid curve is machined exactly, assembly tolerance and thermal expansion can easily cause interference. Designers therefore modify the profile slightly, for example by shrinking or offsetting the curve. This <strong>profile modification</strong> step is necessary, but it also increases design complexity.</p> <h4 id="4-mass-imbalance-and-vibration">4. Mass Imbalance and Vibration</h4> <p>Because the cycloid disk moves eccentrically, its center of mass continuously shifts during rotation. At high speed, this can create serious vibration.</p> <p>To cancel this imbalance, many designs use <strong>two cycloid disks</strong> with a 180-degree phase difference. This improves balance, but it adds weight and volume.</p> <p><a href="/hardware04/">Next post: [Robot Hardware 04] - Actuators (3): QDD Actuators</a></p> <h3 id="references">References</h3> <p>[1] https://mevirtuoso.com/become-a-member-cycloidal/</p> <p>[2] https://www.youtube.com/watch?v=KX9Mx8ghtio</p> <p>[3] https://www.youtube.com/watch?v=yBckAoqNQx4</p>]]></content><author><name></name></author><category term="Hardware Development"/><summary type="html"><![CDATA[Robot hardware from a Physical AI perspective - cycloidal drives]]></summary></entry><entry xml:lang="ko"><title type="html">[로봇 하드웨어 03] - 사이클로이드 감속기</title><link href="https://dongho.dev/hardware03-03-kr/" rel="alternate" type="text/html" title="[로봇 하드웨어 03] - 사이클로이드 감속기"/><published>2025-12-05T00:00:00+00:00</published><updated>2025-12-05T00:00:00+00:00</updated><id>https://dongho.dev/hardware-03-03-kr</id><content type="html" xml:base="https://dongho.dev/hardware03-03-kr/"><![CDATA[<p><a href="/hardware03-kr/">상위 포스트로 돌아가기</a></p> <p>이번 포스트에서는 뛰어난 내충격성과 높은 토크 밀도로 최근 보행 로봇과 소형 액추에이터 분야에서 다시 주목받고 있는 <strong>Cycloidal Drive</strong>에 대해 알아보겠습니다.</p> <p>Cycloidal Drive는 특유의 <strong>Eccentric (편심)</strong> 입력과 그에 따른 사이클로이드 궤적을 이용하여 감속과 토크 증폭을 달성하는 기계 장치입니다. Cycloid Teeth Geometry에 따라 감속비가 결정되며, 매우 컴팩트한 크기에서 높은 토크를 낼 수 있고 무엇보다 충격에 매우 강한 구조적 특성을 가집니다.</p> <h2 id="작동-원리와-감속비">작동 원리와 감속비</h2> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hardware/cycloid_motion-480.webp 480w,/assets/img/hardware/cycloid_motion-800.webp 800w,/assets/img/hardware/cycloid_motion-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hardware/cycloid_motion.gif" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Cycloidal Reduction의 작동 예시 [1] </div> </div> </div> <p>Cycloidal Drive의 작동 원리는 크게 세 단계의 메커니즘으로 이루어집니다.</p> <ol> <li><strong>Eccentric Input:</strong> 모터와 연결된 입력축(Input shaft)에 편심 베어링이 결합되어 있어, 입력축이 회전하면 중심축을 벗어난 Eccentric motion을 만들어냅니다.</li> <li><strong>Cycloid Motion:</strong> 이 편심 베어링에 끼워진 Cycloid Disk는 하우징의 바깥쪽 핀(Ring pins)들과 맞물리며 사이클로이드 궤적을 따라 구르듯 흔들리며 회전합니다.</li> <li><strong>Output Extraction:</strong> Disk에 뚫린 구멍들을 통해 Output shaft의 핀들이 연결되어, Disk의 느린 자전 운동만을 출력축으로 전달합니다.</li> </ol> <h3 id="고정-축에-따른-감속비-gear-ratio">고정 축에 따른 감속비 (Gear Ratio)</h3> <p>Cycloidal Drive는 어떤 요소를 고정하느냐에 따라 감속비와 회전 방향이 달라집니다. 하우징의 핀 개수를 N, Cycloid Disk의 톱니(Lobe) 개수를 N-1이라고 할 때:</p> <ul> <li> <p><strong>하우징(Outer Ring) 고정 / Disk 출력:</strong> 입력축이 1회전할 때, Disk는 톱니 1개 분량만큼 입력축과 <strong>역방향</strong>으로 회전합니다. Gear Ratio = (N - 1) / 1</p> <figure> <iframe width="720" height="405" src="https://www.youtube.com/embed/L6sBhsO_UNA" title="Cycloidal Drive - Fixed Ring" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen=""> </iframe> <figcaption style="text-align: center;"> <em>Cycloidal Drive — Fixed Ring (short)</em> </figcaption> </figure> </li> <li> <p><strong>Disk 고정 / 하우징(Outer Ring) 출력:</strong> 입력축이 1회전할 때, 하우징이 핀 1개 분량만큼 입력축과 <strong>정방향</strong>으로 회전합니다. Gear Ratio = N / 1</p> <figure> <iframe width="720" height="405" src="https://www.youtube.com/embed/7WD3s8bxU5c" title="Cycloidal Drive - Fixed Shaft" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen=""> </iframe> <figcaption style="text-align: center;"> <em>Cycloidal Drive — Fixed Shaft (short)</em> </figcaption> </figure> </li> </ul> <h2 id="2-접촉-메커니즘-설계-rolling-vs-sliding-contact">2. 접촉 메커니즘 설계: Rolling vs. Sliding Contact</h2> <p>기어 이빨의 마찰로 동력을 전달하는 일반적인 기어와 달리, Cycloidal Drive는 기구학적인 구속 조건(Kinematic Constraint)에 의해 궤적이 완전히 결정됩니다. 따라서 설계 목적에 따라 <strong>Rolling Contact</strong>과 <strong>Sliding Contact</strong> 두 가지 방식을 모두 적용할 수 있습니다.</p> <h3 id="rolling-contact-구름-접촉">Rolling Contact (구름 접촉)</h3> <p>하우징 핀이나 출력 핀 위치에 베어링이나 원통형 롤러를 추가하여 마찰을 최소화하는 방식입니다.</p> <ul> <li><strong>특징:</strong> 마찰 손실이 극히 적어 전달 효율이 매우 높고 발열이 적습니다.</li> <li><strong>한계:</strong> 소형화가 어렵습니다. 높은 감속비를 얻으려면 롤러의 개수가 늘어나야 하는데, 이는 곧 부품 수 증가와 감속기 전체 무게의 급격한 상승으로 이어집니다.</li> </ul> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hardware/rolling_contact_cycloid-480.webp 480w,/assets/img/hardware/rolling_contact_cycloid-800.webp 800w,/assets/img/hardware/rolling_contact_cycloid-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hardware/rolling_contact_cycloid.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Rolling Conract Cycloid [2] </div> </div> </div> <h3 id="sliding-contact-미끄럼-접촉">Sliding Contact (미끄럼 접촉)</h3> <p>별도의 롤러 없이 Cycloid Disk의 형상 자체가 하우징이나 출력 핀과 직접 맞닿아 미끄러지며 구동하는 방식입니다.</p> <ul> <li><strong>특징:</strong> 롤러가 생략되므로 구조가 매우 단순해지고 극한의 소형화 및 경량화가 가능합니다.</li> <li><strong>한계:</strong> Sliding Friction(미끄럼 마찰)이 크게 발생하여 동력 전달 효율이 현저히 떨어지고 마모에 취약합니다.</li> </ul> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hardware/sliding_contact_cycloid-480.webp 480w,/assets/img/hardware/sliding_contact_cycloid-800.webp 800w,/assets/img/hardware/sliding_contact_cycloid-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hardware/sliding_contact_cycloid.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Sliding Contact Cycloid [3] </div> </div> </div> <p>실제 액추에이터 설계에서는 하중이 가장 크게 걸리는 중앙의 Eccentric shaft 부위에는 베어링을 삽입하여 Rolling Contact을 구현하고, 상대적으로 선속도가 낮은 감속기 외부의 Disk와 하우징 사이에는 Sliding Contact을 적용합니다.</p> <p>이때 Sliding Contact의 높은 마찰과 마모 문제를 해결하기 위해, Cycloid Disk 자체를 금속 대신 <strong>Delrin (POM)</strong>이나 <strong>Nylon</strong> 같은 저마찰 특성의 엔지니어링 플라스틱으로 가공하여 마찰 손실을 줄이기도 합니다.</p> <h2 id="3-장점-advantages">3. 장점 (Advantages)</h2> <h4 id="1-높은-토크-밀도">1. 높은 토크 밀도</h4> <p>Harmonic Drive와 유사하게 매우 작은 체적으로도 큰 토크를 생성할 수 있어, 관절의 패키징을 컴팩트하게 구성할 수 있습니다.</p> <h4 id="2-높은-내충격성">2. 높은 내충격성</h4> <p>Spur gear가 1~2개의 치형으로 토크를 감당하는 반면, Cycloidal Drive는 구조적으로 전체 치형의 약 <strong>30~50%</strong>가 동시에 하중을 분담합니다 (Load Distribution). 따라서 외부 충격 부하(Shock Load)가 분산되어 기어가 파손되지 않으며, 보행 로봇의 발구름이나 조작 로봇의 충돌 상황 등 반복적인 충격이 가해지는 시스템에 매우 적합합니다.</p> <h4 id="3-단일-스테이지-고감속비">3. 단일 스테이지 고감속비</h4> <p>단일 스테이지 구조만으로 50:1에서 100:1 이상의 높은 감속비를 달성할 수 있어, 다단 감속기로 인한 부피 증가와 시스템 복잡성을 줄일 수 있습니다.</p> <h2 id="4-단점-및-설계-고려사항">4. 단점 및 설계 고려사항</h2> <h4 id="1-가공-난이도">1. 가공 난이도</h4> <p>부품 수가 적어 보이지만, 구조가 작동하기 위한 가공 난이도는 매우 높습니다. 정밀한 사이클로이드 곡선을 오차 없이 구현해야 하므로 고정밀 CNC 가공이 필수적이며, 조립 과정에서도 높은 숙련도가 요구됩니다.</p> <h4 id="2-공차-민감도">2. 공차 민감도</h4> <p>공차(Tolerance)나 조립 불량도 전체 시스템의 성능 저하로 직결됩니다. 공차가 좁으면 <strong>Jamming(물림)</strong> 현상이 발생하여 구동이 불가능해지고, 공차가 넓으면 <strong>Backlash</strong>가 발생하여 위치 정밀도가 떨어집니다.</p> <h4 id="3-profile-modification">3. Profile Modification</h4> <p>편심량(Eccentricity)은 Cycloid Profile의 곡률과 Kinematic Constraint를 결정하는 핵심 변수입니다. 이론적인 사이클로이드 곡선을 그대로 가공하면 조립 공차나 열팽창에 의해 물림 현상이 발생하기 쉽습니다. 따라서 곡선의 크기를 미세하게 줄이거나 수정하는 <strong>Profile Modification</strong> 과정이 반드시 필요하며, 이는 설계의 복잡도를 급격히 높이는 원인이 됩니다.</p> <h4 id="4-질량-불균형과-진동">4. 질량 불균형과 진동</h4> <p>Eccentric motion을 하는 Disk의 질량 중심이 계속 이동하므로 고속 회전 시 심각한 진동이 발생합니다. 이를 상쇄하기 위해 일반적으로 위상이 180도 차이 나는 <strong>두 개의 Cycloid Disk(Dual-disk)를 겹쳐서 사용</strong>하는데, 이 경우 무게와 부피가 추가로 증가하게 됩니다.</p> <p><a href="/hardware04-kr/">다음 포스트 : [로봇 하드웨어 04] - 액추에이터(3): QDD 액추에이터</a></p> <h3 id="references">References</h3> <p>[1] https://mevirtuoso.com/become-a-member-cycloidal/</p> <p>[2] https://www.youtube.com/watch?v=KX9Mx8ghtio</p> <p>[3] https://www.youtube.com/watch?v=yBckAoqNQx4</p>]]></content><author><name></name></author><category term="Hardware"/><summary type="html"><![CDATA[Physical AI 관점에서 본 다관절 로봇 하드웨어 - 사이클로이드 감속기]]></summary></entry><entry xml:lang="en"><title type="html">[Robot Hardware 03] - Planetary Gearboxes</title><link href="https://dongho.dev/hardware03-02/" rel="alternate" type="text/html" title="[Robot Hardware 03] - Planetary Gearboxes"/><published>2025-12-04T00:00:00+00:00</published><updated>2025-12-04T00:00:00+00:00</updated><id>https://dongho.dev/hardware-03-02</id><content type="html" xml:base="https://dongho.dev/hardware03-02/"><![CDATA[<p><a href="/hardware03/">Back to the parent post</a></p> <p>This note covers <strong>planetary gearboxes</strong>. If the Harmonic Drive is strong in high reduction ratio and positioning precision, the planetary gearbox is optimized more for <strong>stiffness</strong> and <strong>durability</strong>: the ability to physically carry load.</p> <h2 id="how-a-planetary-gearbox-works">How a Planetary Gearbox Works</h2> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hardware/pg-480.webp 480w,/assets/img/hardware/pg-800.webp 800w,/assets/img/hardware/pg-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hardware/pg.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Planetary gear structure [1] </div> </div> </div> <p>A planetary gearbox resembles the orbital structure of a solar system. It has four core elements:</p> <ol> <li><strong>Sun Gear:</strong> The input gear at the center.</li> <li><strong>Planet Gears:</strong> Multiple gears that mesh with the sun gear and orbit around it.</li> <li><strong>Ring Gear:</strong> The outer internal gear that surrounds the system.</li> <li><strong>Carrier:</strong> The structure that holds the planet gears together. It often serves as the output.</li> </ol> <h3 id="the-key-idea-load-distribution">The Key Idea: Load Distribution</h3> <p>The most important feature of this structure is <strong>load distribution</strong>.</p> <p>In a simple spur gear pair, one contact patch may have to carry the entire torque. In a planetary gearbox, several planet gears mesh at the same time, distributing both torque and contact stress.</p> <p>If the number of planet gears is $N$, then in an ideal system each gear carries roughly $1/N$ of the total torque.</p> <p>Real systems are never perfectly equal because of machining tolerances and bearing play, but the tooth surface pressure can still be much lower than in a single gear pair.</p> <p>This leads to several advantages:</p> <ul> <li><strong>High torque density:</strong> A compact gearbox can transmit high torque.</li> <li><strong>Shock resistance:</strong> External shock load is distributed across multiple gears, reducing the risk of failure.</li> <li><strong>Improved fatigue life:</strong> Stress on each individual tooth is reduced.</li> </ul> <p>Because of these properties, planetary gearboxes are widely used in joint actuators for dynamic robots where <strong>force control</strong> and robustness matter, such as humanoids and quadrupeds.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hardware/pg-compare-480.webp 480w,/assets/img/hardware/pg-compare-800.webp 800w,/assets/img/hardware/pg-compare-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hardware/pg-compare.gif" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Planetary gearbox motion example [1] </div> </div> </div> <h2 id="gear-ratio">Gear Ratio</h2> <p>Planetary gearboxes can create different gear ratios and rotation directions depending on which element is fixed and which elements are used as input and output.</p> <p>For multi-jointed robots, a common configuration is:</p> <ul> <li><strong>Ring Gear:</strong> Fixed</li> <li><strong>Sun Gear:</strong> Input</li> <li><strong>Carrier:</strong> Output</li> </ul> <p>The gear ratio for this configuration is:</p> \[\text{Gear Ratio} = \frac{\omega_{in}}{\omega_{out}} = 1 + \frac{N_{ring}}{N_{sun}}\] <p>where:</p> <ul> <li>$N_{ring}$: number of teeth on the ring gear</li> <li>$N_{sun}$: number of teeth on the sun gear</li> </ul> <p><strong>Example:</strong></p> <ul> <li>Sun Gear = 20 teeth</li> <li>Ring Gear = 80 teeth</li> </ul> \[\text{Gear Ratio} = 1 + \frac{80}{20} = 1 + 4 = 5:1\] <p>This “+1” term is a structural feature of the planetary arrangement.</p> <hr/> <h2 id="advantages">Advantages</h2> <h4 id="1-high-structural-stability">1. High Structural Stability</h4> <p>Because multiple planet gears share the load, planetary gearboxes are good at repeatedly transmitting high torque. Unlike Harmonic Drives, they do not rely on a thin flexible component, so they are generally more robust under <strong>shock loads</strong>.</p> <h4 id="2-cost-efficiency">2. Cost Efficiency</h4> <p>Compared with Harmonic Drives or cycloidal drives, which require special materials or precise elastic geometry, planetary gearboxes can be manufactured using more conventional spur gear processes. This usually makes them less expensive.</p> <h4 id="3-easier-maintenance">3. Easier Maintenance</h4> <p>The structure is intuitive, and there are no intentionally deforming fatigue-critical parts. With proper lubrication, service life and maintenance are relatively easier to predict.</p> <hr/> <h2 id="limitations">Limitations</h2> <h4 id="1-limited-single-stage-ratio">1. Limited Single-Stage Ratio</h4> <p>A planetary gearbox has a geometric limit on the reduction ratio it can achieve in a <strong>single stage</strong>.</p> <ul> <li>The sun gear cannot become arbitrarily small.</li> <li>The ring gear cannot become arbitrarily large.</li> <li>In practice, a single stage is often limited to around <strong>3:1 to 10:1</strong>.</li> </ul> <p>Higher ratios require multiple stages. Multi-stage gearboxes become longer and heavier, and they accumulate <strong>friction loss</strong> and <strong>backlash</strong>.</p> <h4 id="2-backlash">2. Backlash</h4> <p>Most planetary gearboxes use rigid metal spur gears. To rotate smoothly, gears need a small clearance between teeth, so <strong>backlash</strong> is structurally difficult to eliminate. This hurts precise position control.</p> <h3 id="a-common-improvement-helical-gears">A Common Improvement: Helical Gears</h3> <p>Helical gears are sometimes used to reduce noise, vibration, and backlash. Their teeth are angled, so multiple teeth engage gradually and smoothly, increasing the contact ratio.</p> <p>There are trade-offs:</p> <ol> <li><strong>Axial force:</strong> Because of the tooth angle, rotation creates thrust force along the shaft. This requires thrust bearings and makes the housing more complex.</li> <li><strong>Lower efficiency:</strong> Extra friction from axial force can slightly reduce transmission efficiency compared with spur gears.</li> </ol> <p>Double-helical or herringbone gears can cancel the axial force, but they are much harder and more expensive to manufacture.</p> <p><a href="/hardware03-03/">Next reference note: [Robot Hardware 03] - Cycloidal Drives</a></p> <h3 id="references">References</h3> <p>[1] https://www.tec-science.com/mechanical-power-transmission/planetary-gear/epicyclic-planetary-gear/</p>]]></content><author><name></name></author><category term="Hardware Development"/><summary type="html"><![CDATA[Robot hardware from a Physical AI perspective - planetary gearboxes]]></summary></entry><entry xml:lang="ko"><title type="html">[로봇 하드웨어 03] - 유성기어 감속기</title><link href="https://dongho.dev/hardware03-02-kr/" rel="alternate" type="text/html" title="[로봇 하드웨어 03] - 유성기어 감속기"/><published>2025-12-04T00:00:00+00:00</published><updated>2025-12-04T00:00:00+00:00</updated><id>https://dongho.dev/hardware-03-02-kr</id><content type="html" xml:base="https://dongho.dev/hardware03-02-kr/"><![CDATA[<p><a href="/hardware03-kr/">상위 포스트로 돌아가기</a></p> <p>이번 포스트에서는 <strong>유성기어 감속기 (Planetary Gearbox)</strong>에 대해 알아보겠습니다. 앞서 다룬 Harmonic Drive가 큰 감속비와 높은 위치 정밀도에 강점이 있다면, Planetary Gearbox는 정밀함보다는 <strong>높은 강성(Stiffness)과 내구성(Durability)</strong>, 즉 물리적으로 버티는 힘에 최적화된 감속기입니다.</p> <h2 id="유성기어-감속기의-원리">유성기어 감속기의 원리</h2> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hardware/pg-480.webp 480w,/assets/img/hardware/pg-800.webp 800w,/assets/img/hardware/pg-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hardware/pg.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> 유성기어 구조 [1] </div> </div> </div> <p>Planetary Gearbox는 태양계의 공전 운동을 모사한 구조를 가지고 있으며, 다음 네 가지 핵심 요소로 구성됩니다.</p> <ol> <li><strong>Sun Gear (태양 기어):</strong> 중심에 위치한 입력 기어입니다.</li> <li><strong>Planet Gears (유성 기어):</strong> Sun Gear 주위를 돌며 맞물리는 다수의 기어입니다.</li> <li><strong>Ring Gear (링 기어):</strong> 시스템의 외곽을 감싸는 내부 치형(Internal Gear) 기어입니다.</li> <li><strong>Carrier (캐리어):</strong> 다수의 Planet Gear를 묶어주며, 보통 출력축 역할을 합니다.</li> </ol> <h3 id="핵심은-하중-분산-load-distribution">핵심은 하중 분산 (Load Distribution)</h3> <p>이 구조의 가장 중요한 특징은 <strong>Load Distribution</strong>입니다. 일반적인 Spur Gear 시스템에서는 단 하나의 접촉점(Contact patch)이 모든 토크를 감당해야 합니다. 반면, Planetary Gearbox는 여러 개의 Planet Gear가 동시에 맞물리며 <strong>토크와 접촉 응력(Contact Stress)을 분산</strong>시킵니다.</p> <p>Planet Gear의 개수를 $N$이라 할 때, 이상적인 환경에서는 각 기어가 전체 토크의 $1/N$만큼만 부담하게 됩니다.</p> <p>물론 현실적으로는 가공 오차나 베어링 유격 등으로 인해 완벽한 균등 분산은 어렵지만, 단일 기어 대비 치면압(Surface Pressure)이 획기적으로 낮아지는 것은 분명합니다.</p> <p>이로 인한 이점은 다음과 같습니다.</p> <ul> <li><strong>높은 토크 밀도:</strong> 작은 부피로 큰 토크를 전달할 수 있습니다.</li> <li><strong>내충격성:</strong> 외부 충격(Shock Load)이 여러 기어로 분산되어 파손 위험이 적습니다.</li> <li><strong>피로 수명 증대:</strong> 개별 기어 이빨이 받는 스트레스가 줄어듭니다.</li> </ul> <p>이러한 특성 때문에 Planetary Gearbox는 <strong>힘 제어(Force Control)</strong>가 중요하거나 거친 환경에서 구동되는 <strong>동적 로봇(예: 휴머노이드, 4족 보행 로봇)</strong>의 관절 액추에이터로 널리 사용됩니다.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hardware/pg-compare-480.webp 480w,/assets/img/hardware/pg-compare-800.webp 800w,/assets/img/hardware/pg-compare-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hardware/pg-compare.gif" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> 유성기어 감속기 동작 예시 [1] </div> </div> </div> <h2 id="감속비-계산">감속비 계산</h2> <p>Planetary Gearbox는 Sun, Ring, Carrier 중 어느 요소를 고정(Fixed)하고 어느 요소를 입력/출력으로 쓰느냐에 따라 다양한 감속비와 회전 방향을 만들어낼 수 있습니다.</p> <p>다관절 로봇에서 가장 보편적으로 사용되는 구성은 다음과 같습니다:</p> <ul> <li><strong>Ring Gear:</strong> 고정 (Fixed)</li> <li><strong>Sun Gear:</strong> 입력 (Input)</li> <li><strong>Carrier:</strong> 출력 (Output)</li> </ul> <p>이 구성에서의 감속비 공식은 다음과 같습니다.</p> \[\text{Gear Ratio} = \frac{\omega_{in}}{\omega_{out}} = 1 + \frac{N_{ring}}{N_{sun}}\] <p>여기서:</p> <ul> <li>$N_{ring}$ : Ring Gear의 이빨 수 (Teeth count)</li> <li>$N_{sun}$ : Sun Gear의 이빨 수</li> </ul> <p><strong>예시:</strong></p> <ul> <li>Sun Gear = 20 teeth</li> <li>Ring Gear = 80 teeth</li> </ul> \[\text{Gear Ratio} = 1 + \frac{80}{20} = 1 + 4 = 5:1\] <p>이처럼 Planetary Gearbox는 기어의 이빨 수 비율에 ‘+1’이 더해지는 구조적 특성을 가집니다.</p> <hr/> <h2 id="장점">장점</h2> <h4 id="1-높은-구조적-안정성">1. 높은 구조적 안정성</h4> <p>다수의 Planet Gear가 부하를 분담하므로 High Torque를 반복적으로 전달하는 데 유리합니다. 특히 Harmonic Drive와 달리 얇고 유연한 부품이 없으므로, <strong>충격 하중(Shock Load)</strong>에 대한 신뢰성이 매우 높습니다.</p> <h4 id="2-비용-효율성">2. 비용 효율성</h4> <p>특수 소재나 정밀한 탄성 설계가 필요한 Harmonic Drive나 사이클로이드 감속기(Cycloidal Drive)에 비해, 일반적인 Spur Gear 가공 공정을 활용할 수 있어 제작 비용이 상대적으로 저렴합니다.</p> <h4 id="3-유지보수-용이성">3. 유지보수 용이성</h4> <p>구조가 직관적이고 피로 파괴를 전제로 한 변형 부품이 없기 때문에, 적절한 윤활(Lubrication)만 이루어진다면 장기적인 수명 예측과 유지보수가 수월합니다.</p> <hr/> <h2 id="단점-및-한계">단점 및 한계</h2> <h4 id="1-제한적인-감속비-scalability-limit">1. 제한적인 감속비 (Scalability Limit)</h4> <p>Planetary Gearbox는 기하학적 구조상 <strong>단일 스테이지(Single Stage)</strong>에서 구현할 수 있는 감속비에 한계가 있습니다.</p> <ul> <li>Sun Gear가 너무 작아지거나 Ring Gear가 너무 커져야 하기 때문입니다.</li> <li>통상적으로 스테이지당 <strong>3:1 ~ 10:1</strong> 수준이 한계입니다.</li> </ul> <p>더 높은 감속비가 필요하면 스테이지를 직렬로 쌓아야(Multi-stage) 하는데, 이 경우 전체 길이가 길어지고 무게가 증가하며, <strong>마찰 손실(Friction Loss)</strong>과 <strong>Backlash</strong>가 누적되어 성능이 저하됩니다.</p> <h4 id="2-백래시-backlash">2. 백래시 (Backlash)</h4> <p>대부분의 Planetary Gearbox는 금속 강체인 Spur Gear를 사용합니다. 기어의 원활한 회전을 위해서는 이빨 사이에 최소한의 틈(Clearance)이 필수적이므로, 구조적으로 <strong>Backlash</strong>를 피하기 어렵습니다. 이는 로봇의 정밀 위치 제어를 방해하는 요소가 됩니다.</p> <h3 id="개선책-헬리컬-기어-helical-gear">개선책: 헬리컬 기어 (Helical Gear)</h3> <p>Spur Gear의 소음과 진동, 그리고 Backlash 문제를 줄이기 위해 <strong>헬리컬 기어(Helical Gear)</strong>를 사용하기도 합니다. 헬리컬 기어는 이빨이 사선으로 배치되어 있어, 여러 이빨이 점진적으로 부드럽게 맞물립니다(High Contact Ratio).</p> <p>하지만 여기에도 <strong>Trade-off</strong>가 존재합니다.</p> <ol> <li><strong>축 방향 하중 (Axial Force):</strong> 이빨의 경사각 때문에 회전 시 축을 밀어내는 힘(Thrust force)이 발생합니다. 이를 버티기 위해 고가의 <strong>Thrust Bearing</strong>이 추가로 필요하며 하우징 설계가 복잡해집니다.</li> <li><strong>효율 저하:</strong> Axial force로 인한 마찰 증가로 동력 전달 효율이 Spur Gear 대비 소폭 감소할 수 있습니다.</li> </ol> <p>이를 해결하기 위해 축 방향 힘을 상쇄시키는 <strong>이중 헬리컬(Double Helical / Herringbone)</strong> 구조를 사용하기도 하지만, 이는 가공 난이도와 비용을 급격히 상승시킵니다.</p> <p><a href="/hardware05-kr/">다음 포스트: [로봇 하드웨어 05] - 액추에이터 (5): 같은 전류를 보내도 토크가 출렁이는 이유</a></p> <h3 id="references">References</h3> <p>[1] https://www.tec-science.com/mechanical-power-transmission/planetary-gear/epicyclic-planetary-gear/</p>]]></content><author><name></name></author><category term="Hardware"/><summary type="html"><![CDATA[Physical AI 관점에서 본 다관절 로봇 하드웨어 - 유성기어 감속기]]></summary></entry></feed>